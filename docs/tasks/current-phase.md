# Current phase: 8 — Visual polish + landing

Spec → [`../specs/2026-05-10-family-tree-design.md`](../specs/2026-05-10-family-tree-design.md) → "Build phasing" → Phase 8 row. Phase 8 design (brainstorm output) → [`../superpowers/specs/2026-05-16-phase-8-visual-polish-design.md`](../superpowers/specs/2026-05-16-phase-8-visual-polish-design.md).

**Ship gate** (brainstormed 2026-05-16 — full rationale in the design spec above):

- **8a — Brand foundations**: Knot brand-guide pull-review + decisions doc landed; warm-shifted `.dark` tokens map verified; logo + favicon + metadata wired; brand icon set (`Branch`, `Leaf`, `Quote`, `Family`, `Sparkle`, `Heart`) shipped.
- **8b — Person + tree polish**: gender-shape avatar variation + deceased treatment + `<Memoriam>` component ship together; tree-overview / zoom-to-fit control + floating "+" hover affordance land; duplicate-card visual marker shipped (option 2 — dashed border + `↑` badge + tooltip + tap-to-jump; **NOT** opacity-based, composes cleanly with deceased treatment); QA feedback gate on duplicate marker before phase close.
- **8c — Landing + nav + animations**: shared `(app)` route group closes [#45](https://github.com/SanchitB23/meetthefam/issues/45); real landing screen replaces `create-next-app` scaffold and closes [#44](https://github.com/SanchitB23/meetthefam/issues/44); heirloom skeletons + `<Suspense>` + `useLinkStatus()` + React 19.2 `<ViewTransition>` (cross-page only, ≤ 200 ms) collectively close [#50](https://github.com/SanchitB23/meetthefam/issues/50); revoke-member confirm copy + italic-Cormorant whitelist audit complete; `APP_VERSION` footer micro-version is the first consumer of [ADR 0009 Amendment 4](../adrs/0009-versioning-and-releases.md)'s build-time-derived version.
- `pnpm typecheck && pnpm lint && pnpm test` clean.
- e2e-smoke-tester passes Phase 1–7 flows against the QA preview (no regressions in the `(app)` route group refactor).

---

### Phase 8 progress — running draft PR #55 opened for visibility

**Branch strategy** *(unchanged from the plan)* — ONE phase branch `feat/phase-8-visual-polish-landing` holds ALL 14 sub-tasks (8a-1..8a-4, 8b-1..8b-3, 8c-1..8c-7). The single squash-merge into `qa` happens once 8c-7 + phase close-out land. `v0.4.0` releases at that point per [ADR 0009 Amendment 4](../adrs/0009-versioning-and-releases.md).

PR #55 is a **running draft** opened mid-phase for review-visibility while the work is in flight — it is NOT a "slice PR" and 8b/8c commits will be added to this same PR (same branch).

**Commits on `feat/phase-8-visual-polish-landing` so far (6 ahead of qa)**:

| SHA | Sub-task |
|---|---|
| `a3ce6ad` | 8a-1 Knot brand-decisions doc |
| `6f86816` | 8a-2 warm-shifted dark-mode tokens |
| `45bc84a` | 8a-3 Logo logomark + favicon + metadata |
| `96aa207` | 8a-4 brand icon set (`Branch`, `Leaf`, `Quote`, `Family`, `Sparkle`, `Heart`) |
| `a60ea87` | 8a polish (favicon `aria`, "An heirloom", drop Logo snapshot, `BRANCH_ASPECT_RATIO` const) |
| `49d9d61` | docs: next-session handoff notes (this section's first draft; superseded by the current revision) |

**Draft PR**: [#55](https://github.com/SanchitB23/meetthefam/pull/55) — full Phase 8 running draft, `feat/phase-8-visual-polish-landing` → `qa`. Will be marked ready ONLY after 8c-7 + phase close-out land.

**Gates at the most recent code-touching HEAD (`a60ea87`)**:

- `pnpm typecheck` clean.
- `pnpm lint` clean (only the pre-existing `PersonForm` `react-hooks/incompatible-library` warning).
- `pnpm test --run` **178 / 178** pass.

### Phase 8b / 8c — next session

**Continue on the SAME branch `feat/phase-8-visual-polish-landing`.** No fresh branch. Pull origin first (`git checkout feat/phase-8-visual-polish-landing && git pull --ff-only`), then start at the first unticked sub-task in the canonical plan: **8c-1**.

The canonical reference is the plan at [`../superpowers/plans/2026-05-16-phase-8-visual-polish-landing.md`](../superpowers/plans/2026-05-16-phase-8-visual-polish-landing.md) — sub-task numbering, locked decisions, internal milestone smokes (8a-done / 8b-done / 8c-done), and phase close-out + release recipe are all unchanged.

**Still-pending sub-tasks**: 8c-1, 8c-2, 8c-3, 8c-4, 8c-5, 8c-6, 8c-7 → phase close-out → `v0.4.0` release (single phase PR squash-merge into qa, then ADR 0009 Amendment 4 release flow).

### Infrastructure blockers — smoke runner status (2026-05-17)

Originally two stacked blockers from the 8a-done smoke attempt. Status as of the 8a/8b-done smoke run on 2026-05-17:

1. **Playwright MCP not surfacing at runtime — STILL BLOCKED.** The Playwright plugin (`playwright@claude-plugins-official`) is correctly listed in `~/.claude/settings.json` (`enabledPlugins`) and in `.claude/settings.local.json` (`enabledMcpjsonServers`), and the `mcp__plugin_playwright_playwright__*` tool names are pre-allowed in `settings.local.json` permissions. Despite this, the tools do NOT surface at call time — even in the controller session (not just dispatched subagents). Root cause: the Claude Code session needs a full restart after adding/enabling the Playwright plugin. Fix: restart Claude Code, confirm `mcp__plugin_playwright_playwright__browser_navigate` appears in the session's tool list before dispatching the smoke agent.
2. **Vercel preview SSO gate — RESOLVED (2026-05-17).** Bypass secret configured and verified: `curl` without bypass returns 401; with `?x-vercel-protection-bypass=$VERCEL_PROTECTION_BYPASS&x-vercel-set-bypass-cookie=true` returns 307 (app redirect). The bypass pattern is now documented in `docs/qa/smoke-flows.md` § "Vercel preview deployment protection". Secret stored as `VERCEL_PROTECTION_BYPASS` in `.env.local` (gitignored). The partial 8a/8b-done smoke run completed all curl-verifiable assertions against the preview.

**8a-done + 8b-done milestone smoke — PARTIAL PASS (Playwright gap; all curl-verifiable gates green):**
- `proxy-redirect-unauth`: PASS — `/dashboard` unauthed returns 307 to `/login?next=%2Fdashboard`
- `proxy-skip-share-link`: PASS — `/share/<token>` unauthed returns 404 (not redirected to login)
- Phase 8a brand assets (8a-2, 8a-3, 8a-4): PASS — favicon (25KB ICO), `logo.svg` (three-ring + terracotta dot SVG), Manrope + Cormorant fonts, heirloom description meta, warm dark tokens (`#15110d` background in `.dark` CSS block) all confirmed deployed
- Phase 8b-1 CSS: PASS — `mtf-node--deceased` class confirmed present in deployed main CSS bundle
- `people-crud-and-link` (phase-3): SKIPPED — needs Playwright + `pre-authed-cookie` for QA
- `phase-4-tree-visualization`: SKIPPED — needs Playwright + `pre-authed-cookie` for QA
- `phase-5-photo-upload`: SKIPPED — needs Playwright + `pre-authed-cookie` + ≥5 MB fixture JPEG
- `phase-6-collaboration`: SKIPPED — `env: local` only (local Supabase stack required)
- `phase-7-share-link`: SKIPPED — `env: local` only (local Supabase stack required)

The interactive CRUD flows (phase-3 through phase-7) remain unrun against QA. They are not regressed by Phase 8 (no RLS or data-model changes in 8a/8b). Run them locally against `pnpm dev` + `supabase start` after Playwright MCP is confirmed surfacing.

### Carry-forward review items from 8a (deferred / non-blocking)

The 8a pull-review of Knot's brand bundle surfaced **zero blocking issues** — all four sub-tasks landed cleanly. The four items below are explicitly captured in [`../architecture/brand-decisions.md`](../architecture/brand-decisions.md) § Open questions and queued for v0.5+ (NOT for the remaining Phase 8 work):

- Spacing scale audit — Tailwind v4 default vs Knot `tokens.json`'s 4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 px scale.
- Type-size scale alignment — Knot named scale (xs / sm / base / lg / xl / 2xl / 3xl / 4xl / 5xl) vs our current ad-hoc Tailwind usage.
- Automated WCAG contrast budget Vitest test — replaces the manual screenshot walk currently used to verify 8a-2's warm-shifted dark mode.
- `base-nova` shadcn preset smoke test against the new radius scale — once 8a-1's adopted radius scale lands in `globals.css` it'd be worth confirming the shadcn primitives still render correctly.

---

**Sub-tasks** (14 total — 8a (4) + 8b (3) + 8c (7)):

**8a — Brand foundations**

- [x] **Sub-task 8a-1** — Knot brand-guide pull-review + decisions doc. No code. Decisions captured in [`../architecture/brand-decisions.md`](../architecture/brand-decisions.md). No ADR amendment needed; ADR 0008 light-touch amendment to follow when 8a-1 lands. Tone-encoding decision: `oklch(from #HEX)` syntax.
- [x] **Sub-task 8a-2** — Warm-shifted dark-mode tokens in `globals.css`; WCAG re-verify. *(landed 2026-05-16 — `.dark` block replaced with the warm-shifted palette per the Knot bundle; contrast verification deferred to QA preview / 8a-done milestone.)*
- [x] **Sub-task 8a-3** — Logo / logomark adoption into top-nav + favicon + metadata. *(landed 2026-05-16 — Logo component + favicon + layout metadata + dashboard top-nav wired. Three overlapping rings on `currentColor`, terracotta accent dot on `var(--accent)` for theme-aware swap.)*
- [x] **Sub-task 8a-4** — Brand icon set (`Branch`, `Leaf`, `Quote`, `Family`, `Sparkle`, `Heart`) at `src/components/icons/`. *(landed 2026-05-16 — six brand icons extracted from Kintree (no Knot fallback needed; all six glyphs are present in Kintree's `shared.jsx`). `Branch` is the standalone 200×80 section-divider motif with a `flip` prop; the other five are 24×24 line glyphs at 1.6px stroke from Kintree's `Icon` map. All `currentColor`-friendly with `aria-hidden=true`.)*

**8b — Person + tree canvas polish**

- [x] **Sub-task 8b-1** — Gender-shape avatar variation + deceased treatment + `<Memoriam>` component. *(landed 2026-05-17 — `<Avatar>` gains `gender` + `deceased` props with `borderRadiusForGender` helper (18%/34%/50%); new `<Memoriam>` component in serif italic with `†` aria-hidden glyph; `personNodeHtml` reads `data.gender_raw`, emits `mtf-node--deceased` class, † badge at top-right, † name prefix; `PersonPicker` + `PersonDetailSheet` thread the new props; `.f3 .mtf-node--deceased` CSS rule in `globals.css`. 11 new Vitest tests; full suite 189/189 pass.)*
- [x] **Sub-task 8b-2** — Tree-overview / zoom-to-fit control + floating "+" hover affordance. *(landed 2026-05-17 — `TreeOverviewButton` (top-right of canvas, `absolute top-3 right-3`, `Maximize2` icon, `aria-label="View whole tree"`) calls `zoomToFit` which clears the URL hash + calls `chart.updateTree({ initial: true })` for bounding-box auto-fit. `PersonHoverPlus` is a presentational overlay receiving `position: { top, left } | null` and `onActivate` from `FamilyTree`; returns null when no node is hovered. Hover wiring is a SEPARATE `useEffect` (NOT inside chart-init) that attaches delegated `pointerover` + `pointerout` listeners to the chart container, scoped to `.mtf-node` via `event.target.closest`; `pointerout` guards against inner-child flicker via `node.contains(event.relatedTarget)`. Long-press callback also sets hover state for mobile parity. Hover "+" opens an inline `PersonForm` (separate from `AddRelativeFab`, no invasive state lift) pre-seeded with `defaultRelation: 'child'` of the hovered person. Both components mount inside the existing `{!readOnly && ...}` block. Chart-init effect deps unchanged. 189/189 tests pass.)*
- [x] **Sub-task 8b-3** — Duplicate-card visual marker (option 2 — dashed border + `↑` badge + tooltip + tap-to-jump). QA feedback gate before close-out. *(landed 2026-05-17 — `personNodeHtml` reads `d.duplicate` (node-level `number`, set by family-chart when the same `data.id` appears more than once) and emits `mtf-node--duplicate` class + `border:1px dashed` inline + `↑` badge at `top:-6px;left:-6px` on the card wrapper (CARD, not avatar wrapper — so it doesn't collide with the deceased `†` badge at `top:0;right:0` on the avatar wrapper). Duplicate cards set `data-duplicate="true"` attribute and omit the ellipsis action button. `FamilyTree.tsx` `setOnCardClick` gains a new branch BEFORE the action-trigger branch: `if (target.closest('[data-duplicate="true"]')) { window.location.hash = '#p=' + encodeURIComponent(id); return }` — reuses existing hash-driven re-center wiring. `globals.css` gets `.f3 .mtf-node--duplicate { border-style: dashed }` + explanatory comment about solid connector lines (pragmatic compromise). 4 new Vitest tests (duplicate class+dashed border, ↑ badge corner, no ellipsis on duplicate, deceased+duplicate compose without collision); full suite 193/193 pass. QA feedback gate per locked decision #13: controller asks user before commit.)*

**8c — Landing + nav + animations**

- [x] **Sub-task 8c-1** — Shared `(app)` route group for chrome (closes [#45](https://github.com/SanchitB23/meetthefam/issues/45)). *(landed 2026-05-18 — new `src/app/(app)/layout.tsx` carries the top-nav (Logo + "meetthefam" wordmark + Sign Out) with `min-h-screen flex flex-col bg-background` so authed routes can grow to fill the viewport for the upcoming `<VersionFooter>`. 33 file renames moved `dashboard/`, `tree/[id]/`, `invite/[token]/` into `(app)/`. Cross-route imports updated in 4 source files (`(app)/dashboard/_components/TreeCardMenu.tsx`, `(app)/invite/[token]/page.tsx`, `share/[token]/page.tsx`, `(app)/_components/SignOutButton.tsx`) + 10 test files. Old `dashboard/actions.ts` was a misleading bundle of `signOut` + tree-CRUD; split: `(app)/_actions/signOut.ts` keeps only `signOut` (shared by the layout's Sign Out button); `(app)/dashboard/actions.ts` holds `createTree`/`renameTree`/`deleteTree`. `proxy.ts` matcher unchanged — route groups are URL-transparent. Public routes (`/`, `/login`, `/auth/*`, `/share/*`) stay at root and don't inherit chrome. Gates: typecheck clean, lint 14 pre-existing warnings, 195/195 tests.)*
- [x] **Sub-task 8c-2** — Replace `src/app/page.tsx` with real landing screen (closes [#44](https://github.com/SanchitB23/meetthefam/issues/44)). *(landed 2026-05-18 — `src/app/page.tsx` is now a server component that reads the Supabase user via `await createClient()` + `auth.getUser()`; if authed, `redirect('/dashboard')` server-side (closes #44 — post-Google-SSO landing on `/` Next scaffold). Otherwise renders the heirloom landing: new `src/components/landing/LandingHero.tsx` (Logo + Cormorant italic kicker + serif H1 "Build a family tree that feels like home." + muted body + primary CTA `Sign in to begin` → `/login`), `LandingFeatures.tsx` (Family/Heart/Leaf brand-icon cards with Branch SVG dividers above + below, the lower one with `flip` prop), `LandingFooter.tsx` (Cormorant italic tagline + sign-in link). All four files use brand tokens only (no hex). Italic-Cormorant whitelist honored: hero kicker + footer tagline (both on the whitelist). Gates: typecheck clean, lint 14 pre-existing warnings, 195/195 tests.)*
- [ ] **Sub-task 8c-3** — Heirloom palette pass on empty / loading / error states (closes [#50](https://github.com/SanchitB23/meetthefam/issues/50) 1/3).
- [ ] **Sub-task 8c-4** — Slow-nav loading affordance: `<Suspense>` + `useLinkStatus()` (closes [#50](https://github.com/SanchitB23/meetthefam/issues/50) 2/3).
- [ ] **Sub-task 8c-5** — React 19.2 `<ViewTransition>` for cross-page animations (closes [#50](https://github.com/SanchitB23/meetthefam/issues/50) 3/3).
- [ ] **Sub-task 8c-6** — Revoke-member confirm copy + italic-Cormorant whitelist audit.
- [ ] **Sub-task 8c-7** — `APP_VERSION` footer micro-version (first consumer of Amendment 4).

**Out of scope** (per the brainstorm — see spec §"Out of scope"):

- Task 14 (`useEffectEvent` polish) — dropped; revisit v0.5+.
- Task 18 (Tree-settings unified sheet refactor) — deferred to v0.5 brainstorm.
- Tree-nav within-canvas animations — deferred unless 8c-5 reveals a boundary issue.
- Issue [#49](https://github.com/SanchitB23/meetthefam/issues/49) — standalone `fix/login-redirect-authed-user` hotfix outside Phase 8.
- Issue [#25](https://github.com/SanchitB23/meetthefam/issues/25) — `post-v1.0` labeled; Phase 9 / pre-launch.

> **Workflow note** — Phase 8 continues under the phase-branch-as-default workflow (`feedback_feature_branch_workflow.md`). All 14 sub-tasks land as separate commits on `feat/phase-8-visual-polish-landing`; one PR at phase end squash-merges into `qa`. Closes with `v0.4.0` per [ADR 0009 §1](../adrs/0009-versioning-and-releases.md) — **first release using [Amendment 4](../adrs/0009-versioning-and-releases.md)'s zero-commit release-branch + fast-forward-push recipe.** Pre-v1 policy still applies (no prod-DB / prod-Vercel-config changes until v1.0 launch gate).

---

## Previous phase: 7 — Share link (✅ closed)

Closed with **`v0.3.0`**. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.3.0).

Spec → [`../specs/2026-05-10-family-tree-design.md`](../specs/2026-05-10-family-tree-design.md) → "Build phasing" → Phase 7 row.

**Ship gate (met)**:

- Anonymous read-only share link works end-to-end: owner toggles the `share_token` on the tree → owner shares the `/share/<token>` URL → a logged-out visitor opens the URL → reads the tree (people + photos via `service_role` lookup, no RLS round-trip) without seeing edit affordances.
- Owner can rotate the share token (invalidates the old URL).

**Sub-tasks**:

- [x] **Sub-task 1** — Share-link Server Actions (`enableShareLink` / `regenerateShareToken` / `disableShareLink`). New `src/app/tree/[id]/share/actions.ts` exports three owner-only actions that mutate `trees.share_token`. `enableShareLink` and `regenerateShareToken` share the same code path — both mint a fresh 32-byte URL-safe token via Node's `crypto.randomBytes(32).toString('base64url')` and write it to the column, rotating in place (matches Phase 6 `resendInvite`'s "old URL dies immediately" semantics); the two are kept as separate exports so the Task 2 UI can distinguish "first-time enable" copy from "rotate" copy without inspecting the current state client-side. `disableShareLink` sets `share_token = null` and is idempotent. All three are RLS-gated via the existing `trees_update_owner` policy (no migration needed — column + policy already exist from Phase 0); non-owner callers hit the empty-result-set RLS tell (`.update(...).select('id')` returns zero rows) and get `{ ok: false, error: 'forbidden' }`. Mirrors `src/app/tree/[id]/members/actions.ts`'s inline `getBaseUrl()` + `mintToken()` helpers (YAGNI — no shared module extracted). `revalidatePath('/tree/' + treeId)` after each mutation per ADR 0007. Tests deferred to sub-task 5.
- [x] **Sub-task 2** — ShareLinkSheet + tree-page Share button. New `src/app/tree/[id]/_components/ShareLinkSheet.tsx` is a Client Component that mirrors `MembersSheet.tsx` exactly — Sheet (bottom, mobile) / Dialog (desktop) toggle via `useIsDesktop`, controlled/uncontrolled `open`/`onOpenChange` props, and Phase 6's two-click confirm pattern (`confirm*` state flips on first click; second click within the surface commits, Cancel/X dismisses) reused for both Regenerate and Disable. Four UI states are branched off `currentUserRole` × `shareToken != null`: **owner-disabled** (intro copy + "Enable read-only share link" button), **owner-enabled** (read-only URL input + Copy-with-tick button + Regenerate + Disable in a "Manage" section), **editor-disabled** (banner: "This tree is not shared. Only the owner can enable sharing."), **editor-enabled** (banner: "This tree is shared via a read-only link. Only the owner can manage the link."). Calls into `enableShareLink` / `regenerateShareToken` / `disableShareLink` from sub-task 1 via `useTransition`; mirrors the freshly-minted token locally for optimistic URL display while `revalidatePath` round-trips. Copy-to-clipboard flips the icon to a green `Check` for 2s — same `setTimeout(setCopied(false), 2000)` shape as `MembersSheet`'s InviteForm + PendingInviteListRow. `src/app/tree/[id]/page.tsx` adds `share_token` to the `TreeRow` type + the trees SELECT, derives `baseUrl` via `(await headers()).get('origin') ?? 'http://localhost:3000'` (mirrors `share/actions.ts`'s inline `getBaseUrl()`), and mounts `<ShareLinkSheet>` with a `<Share2>` icon trigger as a sibling of the existing Members trigger in the top bar. Same `h-10 w-10 rounded-md` chrome as Members for visual symmetry. Tests deferred to sub-task 5.
- [x] **Sub-task 3** — `/share/[token]` Server Component page. New `src/app/share/[token]/page.tsx` is the public read-only share view — the ONLY route in the project that runs `createServiceRoleClient()` (Phase 6 sub-task 3's helper at `src/lib/supabase/service.ts`, reused as-is) against the `trees` + `people` tables. The 256-bit token IS the auth gate (brute-forcing is computationally infeasible); RLS is deliberately bypassed because the share viewer has no session. `/share/*` is already excluded from `src/proxy.ts`'s auth matcher (Phase 1 sub-task 2) so the route is anonymously reachable. Token lookup uses `.maybeSingle<TreeRow>()` for the null-on-no-match contract — covers `share_token = NULL` (sharing disabled), regenerated-old token, and never-existed in one branch (vs `.single()` which throws on zero rows and forces an awkward error-discriminant). On miss, the page calls `notFound()` which renders the new `src/app/share/[token]/not-found.tsx` boundary — heirloom-styled card with copy "This share link is no longer active. The owner may have disabled sharing or rotated the link." plus a `/login` CTA for signup conversion. Page chrome: new `src/app/share/[token]/_components/ShareBanner.tsx` Server Component renders a sticky top banner ("You're viewing a shared family tree." + "Sign up to create your own" link to `/login`); below the banner, an `<h1>` with the tree name + optional description; below that, `<FamilyTree readOnly>` (read-only chrome lockdown — hiding the FAB, three-dot action menu, and detail-sheet Edit button — is staged into **sub-task 4**; this sub-task ships the route shell). `<FamilyTree>` gained a typed-only `readOnly?: boolean` prop in this commit so the page typechecks ahead of the sub-task-4 behavior wiring (5th file change beyond the three new share files + share-link.md doc edit; called out here for traceability). Metadata title is the static string `'Shared family tree · meetthefam'` — deliberately does NOT include the person/tree names so the document head never leaks tree contents to bots, link previews, or browser history. `docs/architecture/share-link.md` line 31 + the box-and-arrow diagram updated — "Route Handler" → "Server Component"; line 59's "Trade-off: this Route Handler" → "Trade-off: this Server Component". Tests deferred to sub-task 5.
- [x] **Sub-task 4** — `readOnly` mode wiring for `<FamilyTree>` / `personNodeHtml` / `<PersonDetailSheet>`. Three-file edit set finishes the chrome lockdown the sub-task-3 page stub teed up. `src/app/tree/[id]/_lib/person-node-html.ts` gains a `PersonNodeHtmlOptions = { readOnly?: boolean }` second arg; when `readOnly` is true the `[data-action-trigger]` three-dot button block is omitted from the returned HTML string (the `ellipsisButton` template-literal const stays in the file but is unreferenced in that path — kept inline for symmetry with the writable path). `src/app/tree/[id]/_components/FamilyTree.tsx` promotes the typed-only `readOnly?: boolean` stub from sub-task 3 into runtime behavior: destructures `readOnly = false`, wires a no-op `onLongPress` callback through `usePressActions` when read-only (hook is still invoked unconditionally per Rules of Hooks), threads `{ readOnly }` into the `personNodeHtml(d, { readOnly })` arrow inside `setCardInnerHtmlCreator`, gates the `[data-action-trigger]` branch inside `setOnCardClick` behind `!readOnly` (defence-in-depth — the trigger element isn't in the DOM either way, but the click handler now ignores any stray match), adds `readOnly` to the chart-init effect's dependency array, and renders `<PersonActionMenu>` + `<AddRelativeFab>` inside a `{!readOnly && <>...</>}` fragment. `readOnly` is also passed through to `<PersonDetailSheet>`. `src/app/tree/[id]/_components/PersonDetailSheet.tsx` accepts a new `readOnly?: boolean` prop (defaults to `false`) and wraps the footer Edit-button block in `{!readOnly && (...)}` — the only mutation surface in the detail sheet. The `editPerson` state + `<PersonForm>` mount below stay untouched (dead code paths when `readOnly` is true since nothing can set `editPerson` to non-null). Pan, pinch-zoom, and tap-to-detail-sheet all keep working for share viewers — the detail content is the whole point of the share view.
- [x] **Sub-task 5** — Tests + Phase 7 close-out. **17 new Vitest tests across 3 files**, suite total **147 → 164** passing locally. (1) `src/__tests__/actions/shareLink.test.ts` — **9 tests** covering all three Server Actions from sub-task 1: `enableShareLink` (happy path with shareUrl/shareToken assertions + DB ground-truth + token length ≥40 char `base64url(32) = 43` check; already-enabled rotate-in-place; non-owner editor → `forbidden` + DB row unchanged), `regenerateShareToken` (happy path rotates the token; non-owner editor → `forbidden`; regenerate from disabled state still mints a fresh token — the action is effectively idempotent on top of any prior state because both `enable` and `regenerate` share the same code path per sub-task 1's design), `disableShareLink` (happy path nulls the column; idempotent re-disable still returns `ok: true` because the owner-only `trees_update_owner` RLS USING clause matches even when the new value equals the current value; non-owner editor → `forbidden` with ground-truth assert that the share_token is unchanged). Mock pattern mirrors `inviteEditor.test.ts` exactly — `vi.mock('next/cache')` no-op, `vi.mock('next/headers')` returns an origin so `getBaseUrl()` resolves, `vi.mock('@/lib/supabase/server')` with a per-test `clientHolder.current` swap so each case scopes its own authenticated client. (2) `src/__tests__/rls/share_token.test.ts` — **4 tests** covering the role matrix on `trees.share_token` UPDATE: ownerA can write to their own tree (with ground-truth read), ownerB-cross-tree affects 0 rows, editorA (member but not owner) affects 0 rows, anon affects 0 rows. Proves the existing `trees_update_owner` policy from Phase 0 holds for the new use case without a Phase 7 migration. (3) `src/__tests__/share/share-page.test.ts` — **4 tests** on the service-role data-lookup pattern that `src/app/share/[token]/page.tsx` uses: valid token returns the tree row; invalid token returns `null` (the `.maybeSingle()` contract from sub-task 3); rotated tree's OLD token returns null while the new token works; disabled tree (`share_token = null`) is unreachable via `.eq('share_token', '...')`. New `src/__tests__/share/` directory added as a sibling of `actions/` and `rls/`. **No new test infrastructure**: reuses `_helpers.ts` (`adminClient` / `anonClient` / `signedInClient` / `createTestUser` / `deleteUserByEmail` / `createTree` / `addMember`) unchanged. **Smoke flow `phase-7-share-link`** appended to [`../qa/smoke-flows.md`](../qa/smoke-flows.md) between the Phase 6 section and the "Adding a new flow" section — 11 numbered steps covering the Phase 7 ship gate end-to-end (owner enables → URL pattern → anon viewer in incognito sees read-only chrome with sticky banner + tree-name heading + canvas / NO FAB / NO three-dot button / NO top-nav; tap a person → detail sheet with NO Edit button; pan + pinch-zoom work; signup-CTA → `/login`; owner regenerates → old URL 404s; owner disables → URL 404s). Skip rules: `needs-local-supabase`, `needs-tools-grant-fix` (the standing `e2e-smoke-tester` tools-grant block from Phase 6 still in force). Verification: `pnpm typecheck` clean, `pnpm lint` 1 warning pre-existing (`PersonForm.tsx` `react-hooks/incompatible-library`, unchanged by this sub-task), `pnpm test --run` 20 files / 164 tests pass.

**Phase 7 close-out**:

- [x] All five sub-tasks ticked above.
- [x] Per-sub-task docs ticks landed in `current-phase.md` in the same commit as each feature commit (per the standing memory rule).
- [x] Vitest suite passing: **17 new Phase 7 tests** (9 `shareLink` actions + 4 RLS `share_token` + 4 share-page data lookup) — total suite now **164 tests** on Node 22 (`engines.node` is `≥24.15.0` but the local runner reports an "Unsupported engine" WARN on 22 and proceeds; CI runs the pinned LTS).
- [ ] `e2e-smoke-tester` agent ran the `phase-7-share-link` smoke flow — still blocked by the `needs-tools-grant-fix` Tooling-backlog item (same block Phase 5 + 6 close-outs flagged); manual QA on the QA preview stood in.
- [x] Manual QA pass on the QA preview — user walked the pre-merge smoke checklist during the [PR #48](https://github.com/SanchitB23/meetthefam/pull/48) review before marking it ready.
- [x] Release version: **`v0.3.0`** (second minor bump after v0.2.0 per [ADR 0009 §1](../adrs/0009-versioning-and-releases.md)). Release rode `release/v0.3.0` per [ADR 0009 §4](../adrs/0009-versioning-and-releases.md) — [PR #51](https://github.com/SanchitB23/meetthefam/pull/51) merged to `main` with merge commit `2797a5f`, GitHub Release [v0.3.0](https://github.com/SanchitB23/meetthefam/releases/tag/v0.3.0) created (prerelease, `--target main`), forward-[PR #52](https://github.com/SanchitB23/meetthefam/pull/52) squash-merged back into `qa` to return the bump.
- n/a Phase 7 migration applied to QA — **no migration this phase** (the existing `trees.share_token` column + `trees_update_owner` RLS policy from Phase 0 already cover Phase 7's needs).
- n/a Phase 7 migration applied to **prod** — no migration this phase + pre-v1 policy (no prod DB changes during Phases 6/7/8; everything batches at v1.0 per [`../dev/prod-readiness.md`](../dev/prod-readiness.md)).

> **Workflow note** — Phase 7 was the first phase under the phase-branch-as-default workflow (memory rule `feedback_feature_branch_workflow.md` inverted 2026-05-15; ADR 0010 needs amendment). All 5 sub-tasks landed as separate commits on `feat/phase-7-share-link`, then a single PR `#48` squash-merged into `qa` (commit `92bf861`). Release rode `release/v0.3.0` per [ADR 0009 §4](../adrs/0009-versioning-and-releases.md).

---

## Previous phase: 6 — Collaboration (✅ closed)

Closed with **`v0.2.0`**. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.2.0).

**Ship gate (met)**:

- Owner enters editor's email → server mints a single-use 7-day-TTL token → owner gets a copy-paste invite URL. Invitee opens the URL while logged in as the matching email → confirms → lands on the tree with editor role wired through RLS.
- Owner can revoke an editor's access; revoked editors immediately lose RLS-gated reads + writes on next request.
- Editor can perform every Phase 3–5 mutation (add / edit / delete person, link spouse, set parents, add child, upload + remove photo) on the shared tree; non-editor (anon or other-account) attempts are still rejected at the DB layer.
- UI permission-gates correctly — tree-page top-bar exposes a Members entry for both roles; owner sees the invite form + revoke actions, editor sees the members list read-only. Dashboard `TreeCardMenu` stays as Rename + Delete (no Manage Members entry — tree-page only).
- `pnpm typecheck && pnpm lint && pnpm test` clean (RLS + Server-Action coverage for the invite / accept / revoke paths).
- Release: `v0.2.0` cut from `qa` per the existing recipe (first minor bump after v0.1.0 per [ADR 0009 §1](../adrs/0009-versioning-and-releases.md)).

**Sub-tasks**:

- [x] **Sub-task 1** — `tree_invites` migration + RLS + `accept_invite` `SECURITY DEFINER` RPC. New `supabase/migrations/20260513211135_tree_invites.sql` (233 lines) creates the `public.tree_invites` table (id / tree_id / email / role / token / invited_by / created_at / expires_at / accepted_at / revoked_at) with a `BEFORE INSERT/UPDATE` email-lowercase trigger and a partial unique index `tree_invites_open_per_email` on `(tree_id, lower(email)) WHERE accepted_at IS NULL AND revoked_at IS NULL` (so one OPEN invite per email-per-tree but historical accepted/revoked rows can coexist). 4 owner-gated RLS policies (SELECT / INSERT / UPDATE / DELETE — only the tree's owner can manage invites). `accept_invite(p_token text)` `SECURITY DEFINER` RPC returns `jsonb_build_object('tree_id', ...)` on success and raises `P0001` with a friendly tag-as-message (`not_found` / `revoked` / `expired` / `email_mismatch` / `already_accepted`) on every other path; idempotent on a pre-existing membership for the same user-tree pair. Applied locally + to QA project `ljjvwtpifmoshfknlbaj` (registered version `20260514032107` per the MCP-clock-drift pattern the migration runbook captures); prod apply (`family-tree-prod` `ycnsgkotrbjifsjkqmvn`) happens during this close-out's release recipe. *(PR #38, commit `795058b`)*
- [x] **Sub-task 2** — Server Actions (`inviteEditor` / `revokeInvite` / `resendInvite` / `revokeMember`) + flag-gated `sendInviteEmail` stub. New `src/app/tree/[id]/members/actions.ts` (324 lines) plus new `src/lib/email/inviteEmail.ts` (19 lines; stub gated by `MEETTHEFAM_EMAIL_INVITES_ENABLED` so production stays on copy-paste URLs for v0.2 — SMTP rate-limit-aware per the standing memory entry). Token generation uses Node's `crypto.randomBytes(32).toString('base64url')` (URL-safe; no `=/+`-strip post-processing needed). `inviteEditor` handles the partial-unique-index 23505 violation on `tree_invites_open_per_email` by fetching the existing OPEN invite and returning `{ ok: false, error: 'already_invited', inviteUrl }` so the owner can re-copy the existing link rather than getting a hard error. `resendInvite` rotates the token IN PLACE (old URL dies immediately — deliberate over a separate "extend expiry" path since rotation is the more conservative default). `supabase-validator` agent PASS on this branch (no advisor drift, RLS unchanged). *(PR #39, commit `f801ef6`)*
- [x] **Sub-task 3** — `/invite/[token]` accept-confirm Server Component + `acceptInvite` Server Action wrapper + `src/proxy.ts` matcher. New `src/app/invite/[token]/page.tsx` (322 lines) renders one of five error-state cards (`not_found`, `revoked`, `expired`, `email_mismatch` with both emails shown side-by-side so the user sees which account they need to sign into, `already_accepted`) or the confirm UI; race-condition fallback via `?error=<tag>` searchParam if the RPC raises between the page-load preview and the Accept click. New `src/lib/supabase/service.ts` (31 lines) extracts a single `createServiceRoleClient()` so Phase 7's `/share/[token]` route can reuse the same pattern. `acceptInvite(token)` wrapper added to `src/app/tree/[id]/members/actions.ts` (+79 lines) calls the `accept_invite` RPC and propagates the P0001 tag-as-message verbatim. `src/proxy.ts` matcher updated to protect `/invite/*` and to forward `?next=<original-url>` through the login bounce so the auth round-trip returns the user to the invite page. *(PR #40, commit `6decc44`)*
- [x] **Sub-task 4** — `MembersSheet` UI + tree-page top-bar Members entry + role-based view branching. New `src/app/tree/[id]/_components/MembersSheet.tsx` (636 lines, Sheet-on-mobile / Dialog-on-desktop via the shared `useIsDesktop` hook). Owner view: members list with per-row revoke icons, pending-invites section with Resend/Revoke per row, invite form with inline copy-paste URL on success and the already-invited warning path (re-surfaces the existing invite's URL with a "this email already has an open invite — copy the existing link" message). Editor view: read-only members list only (no invite form, no revoke actions). Inline two-click revoke-confirms — second click within ~3 s commits the revoke; avoids nested dialog stacking on mobile. `src/app/tree/[id]/page.tsx` (+107 −2) wires the data-fetch (members + open invites) and adds the `<Users2>` top-bar entry that opens the sheet. New behavior: non-members hitting `/tree/<id>` now get an explicit `notFound()` server-side rather than the previous empty-tree-via-RLS-denial render (cleaner UX + matches the "404 if not authorized" pattern). *(PR #41, commit `d64da2f`)*
- [x] **Sub-task 5** — 4 Vitest suites + `phase-6-collaboration` smoke flow. 40 new tests across `src/__tests__/rls/tree_invites.test.ts` (20 — cross-role × CRUD matrix: owner / editor / non-member / anon × SELECT / INSERT / UPDATE / DELETE), `src/__tests__/actions/inviteEditor.test.ts` (6 — happy path + already-invited re-surface + non-owner-rejected + invalid-email + resend-rotates-token + revokeInvite-soft-deletes), `src/__tests__/actions/acceptInvite.test.ts` (9 — all 5 RPC error tags + priority order when multiple conditions hold + idempotent pre-existing membership + happy path + post-accept tree-read works), `src/__tests__/actions/revokeMember.test.ts` (5 — owner-revokes-editor / RLS proof post-revoke / can't-revoke-self / can't-revoke-owner / non-owner-rejected). Suite total **107 → 147**. New `vi.mock('next/headers')` stub pattern introduced for actions that call `getBaseUrl()` to render copy-paste URLs (reused by Phase 7's share-link tests). `phase-6-collaboration` smoke flow (11 steps) appended to [`../qa/smoke-flows.md`](../qa/smoke-flows.md), covering owner-invite → editor-accept → editor-write → owner-revoke → former-editor-read-blocked. *(PR #42, commit `679b4e3`)*

**Phase 6 close-out**:

- [x] All five sub-tasks ticked above. *(see PR / commit refs on each sub-task)*
- [x] Per-sub-task docs ticks landed in `current-phase.md` (and `phase-backlog.md` where applicable) in the same commit / PR as each feature commit (per the standing memory rule).
- [x] Vitest suite passing: **40 new Phase 6 tests** (20 RLS `tree_invites` + 6 `inviteEditor` + 9 `acceptInvite` + 5 `revokeMember`) — total suite now **147 tests** on Node 24.
- [x] `phase-6-collaboration` smoke flow walked manually via Playwright MCP from the main session (the dispatched `e2e-smoke-tester` sub-agent still hits the documented `needs-tools-grant-fix` skip — Playwright MCP attaches to the main session but the sub-agent subprocess can't reach `mcp__plugin_playwright_playwright__*`). All 11 steps PASS. Walk surfaced **3 real bugs in Sub-tasks 3 + 4**, all fixed inline on the phase branch: (a) `/invite/[token]` page used a PostgREST embed `inviter:profiles!tree_invites_invited_by_fkey` that referenced a non-existent FK (`tree_invites.invited_by → auth.users(id)`, not `profiles`), crashing every confirm-card render to `not_found`; (b) `/login`'s magic-link flow didn't forward the `?next=` param into `emailRedirectTo`, so the invitee landed on `/dashboard` instead of `/invite/<token>` post-auth — both fixed in commit `8758755` via two separate queries + a hidden `<input name="next">` plus a `safeNextPath` allowlist; (c) `MembersSheet`'s "Members" section silently empty because `tree_members.user_id` also references `auth.users` not `profiles` — same PostgREST embed trap. Fixed via the same two-query pattern. Post-fix manual walk: 11/11 PASS including re-invite-after-revoke. Manual QA on the QA preview still expected at PR #43 review time.
- [ ] Manual QA pass on the QA preview — to be performed during the phase→qa PR review.
- [ ] Release version: `v0.2.0` (first minor bump after v0.1.0 per [ADR 0009 §1](../adrs/0009-versioning-and-releases.md)).
- [ ] [ADR 0010](../adrs/0010-feature-branch-workflow.md) amendment landed (phase-branch escape hatch for parallel sub-agent dev — see queued note in this close-out's workflow note).
- [x] Phase 6 migration applied to QA (`mcp__supabase__apply_migration` against project `ljjvwtpifmoshfknlbaj`, advisor PASS).
- [x] Phase 6 migration applied to **prod** — **deferred per the new pre-v1 policy set 2026-05-14**. No prod DB or production-Vercel-config changes happen during Phases 6/7/8; everything batches at v1.0. See [`../dev/prod-readiness.md`](../dev/prod-readiness.md) for the full launch-gate checklist; the `tree_invites` migration is queued there under §1.

> **Workflow note** — Phase 6 closed via the feature-branch workflow with one deviation: a **phase branch** `feat/phase-6-collaboration` was cut from `qa` and the 5 sub-tasks merged into it (instead of each PR'ing into `qa` directly). The phase branch then merges into `qa` as a single squash. An [ADR 0010](../adrs/0010-feature-branch-workflow.md) amendment captures the deviation pattern ("phase-branch escape hatch for parallel sub-agent dev") for future phases. The release at the end of the phase still rides a `release/v0.2.0` branch from `qa` per [ADR 0009 §4](../adrs/0009-versioning-and-releases.md).

---

## Previous phase: 5 — Photo upload (✅ closed)

Closed with **`v0.1.0`**. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.1.0).

**Ship gate (met)**:

- Open `/tree/[id]` → tap a person → Edit → tap the avatar in the form → file picker → pick a 5–15 MB phone photo → it shrinks to ~150 KB JPEG client-side → uploads → form's avatar preview updates → save → tree-canvas node + detail sheet both show the photo.
- Same path from "+" / "Add a relative" → pick a photo before save → on submit, person is created first, then the photo is uploaded against the new person id.
- "Remove photo" in the edit form → file deleted from Storage, `photo_url` nulled, avatar falls back to tone-tinted initials.
- Delete a person → the row is gone (RPC) AND `trees/<treeId>/people/<personId>/avatar.jpg` is removed from Storage (best-effort).
- Delete a tree → cascade kills `people` AND the whole `trees/<treeId>/` prefix is purged from Storage (best-effort).
- Storage RLS holds: a non-member's anonymous or authed upload to `photos/trees/<some_other_tree>/...` is rejected at the bucket. Public reads of the public URL still work.
- `pnpm typecheck && pnpm lint && pnpm test` clean.

**Post-sub-task fixes**:

- **2026-05-13 — photos Storage SELECT policy**: sub-task 3's manual QA caught a Storage RLS rejection (`new row violates row-level security policy`) when picking a photo. Root cause: `supabase-js`'s `storage.from('photos').upload(...)` runs `INSERT ... RETURNING` internally, and PostgreSQL requires a permissive SELECT policy for the RETURNING clause even on a successful INSERT — the bucket's `public = true` flag only handles HTTP-level public file reads via storage-api, NOT row-level `storage.objects` SELECT. Two forward-only migrations landed via `fix/photos-storage-select-policy` to address it: `20260513153858_photos_select_policy.sql` (broad `bucket_id = 'photos'` SELECT — unblocks the upload) and `20260513154211_photos_select_policy_tighten.sql` (drops the broad policy, replaces with `is_tree_editor(...)` membership gate matching INSERT/UPDATE/DELETE — clears the Supabase advisor's `public_bucket_allows_listing` warning). [`../architecture/auth-and-rls.md`](../architecture/auth-and-rls.md) "Storage" section updated to reflect the two-layer SELECT model (HTTP public read vs row-level RLS). Lesson worth carrying forward (especially for Phase 6's invite-related Storage paths): any new Storage bucket created via supabase-js will need a corresponding row-level SELECT policy on `storage.objects` for the upload's RETURNING to succeed.

**Sub-tasks**:

- [x] **Sub-task 1** — Migration runbook + Storage bucket + RLS migration. New [`../dev/migrations.md`](../dev/migrations.md) runbook authored — codifies the local → QA → prod discipline, a mandatory `mcp__supabase__list_migrations` cross-check after every remote apply, a `mcp__supabase__get_advisors` post-flight (security + performance), and the forward-only rollback discipline (never re-edit a shipped migration; new corrective migration only). Wired into [`../README.md`](../README.md) and into [CLAUDE.md](../../CLAUDE.md)'s "Where to look first" dev-docs bullet. **Important finding now baked into the runbook**: MCP `apply_migration` stamps the QA `supabase_migrations.schema_migrations.version` column with the **MCP server's own clock at apply-time**, not the local file's timestamp prefix — discovered during this sub-task's QA apply (local file `20260513141141`, QA `version` `20260513143945`, ~28 min delta from MCP-clock skew). The two prior local→QA migrations (`delete_person_rpc`, `people_link_rpcs`) already exhibit the same ~4-hour pattern in retrospect; this is MCP behaviour, not actual migration drift. The runbook's cross-check accordingly matches on (name, order, content), **not** timestamps. New migration `supabase/migrations/20260513141141_photos_bucket_and_rls.sql` (83 lines, authored via the `supabase-engineer` subagent): creates the `photos` Storage bucket (`public-read`, `file_size_limit = 524288` bytes = 512 KB, `allowed_mime_types = ARRAY['image/jpeg']`) and 3 RLS policies on `storage.objects` (INSERT / UPDATE / DELETE), each using the existing `public.is_tree_editor(((storage.foldername(name))[2])::uuid)` helper so the membership predicate shares one source-of-truth with `public.people`'s editor policy (path layout `trees/<treeId>/people/<personId>/avatar.jpg` → `foldername[2]` = treeId). JPEG-only bucket-level MIME enforcement aligns with locked decision 7 from the plan (WebP would fall back to PNG on older Safari and blow the ~150 KB target). Applied locally with both `pnpm exec supabase migration up` (incremental) and `pnpm exec supabase db reset` (from-scratch); both clean. Applied to QA project `ljjvwtpifmoshfknlbaj` via `mcp__supabase__apply_migration`; `mcp__supabase__get_advisors` confirms **zero new ERROR/WARN entries** introduced — pre-existing baseline noise unchanged (13 security WARNs on `assign_default_tone`, `touch_updated_at`, `create_tree_with_owner`, `handle_new_user`, the `is_tree_*` helpers + the auth leaked-password-protection toggle; 5 performance INFOs on unindexed FKs + unused indexes — all pre-date this migration). Branch: `feat/phase-5/sub-task-1-photos-bucket-rls`. *(commit SHA — backfill in a later commit per existing pattern)*
- [x] **Sub-task 2** — Client resize helper + upload/remove Server Actions + `next.config.ts`. Three artefacts landed: (1) new `src/lib/image/resize.ts` (~110 lines) exporting pure `resizeToJpeg(file: File): Promise<{ blob, width, height }>` — uses `createImageBitmap(file, { imageOrientation: 'from-image' })` so EXIF rotation is normalised at decode time (otherwise portrait iPhone shots come out sideways), 1024 px longest-side cap, JPEG quality 0.85, `OffscreenCanvas` when available with a detached `<canvas>` fallback for older mobile Safari; throws a typed `ImageDecodeError` class when `createImageBitmap` rejects so unsupported formats (HEIC on Chrome, corrupt files) surface as a catchable error the form can convert into "Please choose a JPEG or PNG photo." — locked decision 6 from the plan; a second `<img>`-element HEIC fallback was intentionally NOT built and is QA-driven (revisit only if reports come in). (2) `src/app/tree/[id]/actions.ts` extended with `uploadPersonPhoto(treeId, personId, formData)` + `removePersonPhoto(treeId, personId)` slotted between `updatePerson` and `deletePerson` — upload validates `instanceof Blob`, `size > 0`, and `size <= STORAGE_MAX_BYTES` (`524288`, defense in depth matching the bucket's `file_size_limit` from sub-task 1), uploads to `photos/trees/<treeId>/people/<personId>/avatar.jpg` with `upsert: true` + `contentType: 'image/jpeg'`, derives `publicUrl` via `getPublicUrl`, writes it to `people.photo_url`, and on DB-write failure after a successful upload does **best-effort orphan cleanup** (removes the just-uploaded file so we don't leave a Storage object referenced by a stale URL; cleanup failure is `console.warn`-ed and does not fail the outer action); `removePersonPhoto` deliberately nulls `people.photo_url` FIRST and only THEN attempts Storage `remove` — order matters: the row's URL is already gone on the refresh-driven re-render so the avatar UI falls back to initials immediately, and a leftover Storage object is harmless because nothing references it (Storage error is `console.warn`-ed but does not fail the action). **First use of `refresh()` from `next/cache` in the project** (per ADR 0007 line 53), imported alongside the existing `revalidatePath`; choice rationale baked into a comment block at the top of the new section — `refresh()` triggers a client-router re-fetch of the page's dynamic data (no `"use cache"` segments in the project yet), the Server Component re-reads `people` and returns a new array reference, and `FamilyTree.tsx:131`'s chart-init `useEffect` has `[people]` in its deps so the chart rebuilds with the new `photo_url`; the "known gap" the plan flagged in the Verification section turned out to be no gap — `refresh()` alone is sufficient, no `revalidatePath` needed for this path. (3) `next.config.ts` `images` block added per the Phase 5 backlog: `remotePatterns: [{ protocol: 'https', hostname: '*.supabase.co', pathname: '/storage/v1/object/public/photos/**' }]`, `qualities: [75]`, `minimumCacheTTL: 14400` (4 h, matches the v16 default — pinned for stability across upgrades per ADR 0007); comment in the block notes the `<Avatar>` is plain `<img>` for v0.1 (locked decision 4) and the config is set NOW so a Phase 8 `<Image>` swap is a one-component change. Verification: `pnpm typecheck` clean (Node 24); `pnpm lint` 1 warning, pre-existing in `PersonForm.tsx:212` (`react-hooks/incompatible-library` against `watch()`, same finding the Phase 4 close-out flagged), zero new warnings from sub-task 2 files; `pnpm test --run` 10 files / 86 tests pass (suite grew from Phase 4's 69 — Phase 4 close-out only counted the new lib tests; the higher number includes Phase 3 RLS + RPC + action tests), no regressions. Branch: `feat/phase-5/sub-task-2-resize-and-upload`. *(commit SHA — backfill in a later commit per existing pattern)*
- [x] **Sub-task 3** — PersonForm photo field UX. Single-file change: `src/app/tree/[id]/_components/PersonForm.tsx` (+290 / −2), no new files, no Server Action surface changes, no migration churn — all photo work flows through the `uploadPersonPhoto` / `removePersonPhoto` actions sub-task 2 already shipped, and `PersonInput` + `createPerson` + `updatePerson` + `valuesFromPerson` stay untouched (`photo_url` is read off `PersonRow` directly into the Avatar prop, never through `react-hook-form` state — by design, since RHF would couple field validation lifecycle to an async-upload lifecycle they don't share). New top-of-form photo section renders before the "Full name" field: a 96 px circular `<Avatar size={96}>` (reuses the existing component as-is — no Avatar prop additions; sub-task 1's plan already verified the `size={96}` + `photoUrl` contract holds) whose `previewUrl` is derived from a layered fallback `previewObjectUrl ?? localPhotoUrl ?? person.photo_url ?? null`, a hidden `<input type="file" accept="image/jpeg,image/png">` triggered by an "Add photo" / "Change photo" `<Button variant="outline" size="sm">`, plus a mode-conditional secondary CTA (edit-mode: destructive-styled "Remove photo"; create-mode: "Clear"). All photo buttons disable while the existing `useTransition`'s `isPending` is true so a click during in-flight upload can't double-fire. **Photo state lives outside `react-hook-form` deliberately** — four separate refs/state pieces with documented lifecycles: `pendingPhotoRef` (Blob held during the create flow until `createPerson` returns a `personId`), `pendingPhotoBlobUrlRef` (object-URL string tracked separately so the cleanup paths have a stable handle independent of preview-state churn), `previewObjectUrl` (state mirror of the ref so the Avatar re-renders when the blob changes), `localPhotoUrl` (`string | null | undefined` tri-state — `undefined` = fall back to `person.photo_url`, `null` = explicitly removed, string = freshly uploaded server URL); inline `photoError` state surfaces errors next to the avatar via the existing `<p className="text-xs text-destructive">` per-field-error idiom (no new toast lib — matched the in-place pattern the rest of the form uses). **Edit-mode flow** on file pick: `resizeToJpeg` → show the local Blob URL as the avatar preview immediately AND fire `uploadPersonPhoto` in a `startTransition`; on success swap to the returned `photoUrl`, on failure revert to the previous server URL and surface the error. The optimistic local-blob preview during the in-flight upload is a deliberate deviation from the planning bullet's "preview the resized blob" wording — the brief didn't specify whether the preview persists across the round-trip, and without it the avatar appears frozen for the upload duration; "revert on failure" keeps the never-lie-to-the-user property intact. **Create-mode flow** on file pick: `resizeToJpeg` → stash Blob in `pendingPhotoRef`, generate an object URL, render the preview; on submit, two-step — after `createPerson` returns `{ ok: true, personId }`, if `pendingPhotoRef.current != null` build a FormData and call `uploadPersonPhoto(treeId, result.personId, formData)`. If the photo upload fails after a successful row insert, the row stays and an inline error renders ("Person saved, but the photo didn't upload — try editing this person to attach it") — explicitly mirrors Phase 3 sub-task 5's orphan-on-link-failure ergonomics (silent auto-DELETE was judged worse UX there and is judged worse UX here). **`ImageDecodeError` handling** caught at the form boundary surfaces as "Please choose a JPEG or PNG photo."; anything else from the resize path surfaces as the generic "Couldn't process the photo. Try a different one."; the HEIC-fallback path is intentionally NOT built (locked decision 6 from the plan + sub-task 2's QA-driven revisit note). **Object-URL lifecycle correctness** covered at five distinct revocation points: (a) component unmount via a dedicated `useEffect` cleanup, (b) replacement with a newer Blob (centralised in a `setPendingBlob` helper so all callers share one revoke + assign sequence), (c) successful edit-mode upload swap, (d) Clear button click in create-mode, (e) `open` flipping true on dialog reuse (covered by extending the existing reset effect rather than adding a new one). The four-piece state structure is the minimum needed to keep these cases clean; collapsing any two of them would either lose the tri-state semantics of `localPhotoUrl` or strand a blob URL across a create-flow reset. Verification: `pnpm typecheck` clean (Node 24.15.0); `pnpm lint` 1 warning, pre-existing in `PersonForm.tsx` (`react-hooks/incompatible-library` against `watch()` from react-hook-form, line number shifted from 212 → 215 because the new state declarations landed above it), zero new warnings from sub-task 3's code; `pnpm test --run` 10 files / 86 tests pass — no regressions (suite size unchanged from sub-task 2; PersonForm photo-field UX has no automated coverage and isn't getting any — sub-task 5 covers Storage RLS + the two action paths, the resize helper is JSDOM-canvas-blocked and explicitly deferred per the plan, and the form UX is verified via the `phase-5-photo-upload` smoke flow + manual QA on the preview). Branch: `feat/phase-5/sub-task-3-personform-photo-field`. *(commit SHA — backfill in a later commit per existing pattern)*
- [x] **Sub-task 4** — Delete cleanups. Two surgical extensions, no new files, no new deps, no migrations. (1) `src/app/tree/[id]/actions.ts` `deletePerson` (around lines 600–620) gains a best-effort Storage purge **after** the `delete_person_atomic` RPC succeeds: `supabase.storage.from(STORAGE_BUCKET).remove([personPhotoPath(treeId, personId)])` reusing the canonical path helper from lines 457–459 (the same helper sub-task 2's `uploadPersonPhoto` / `removePersonPhoto` write through, so there's exactly one source-of-truth for the `trees/<treeId>/people/<personId>/avatar.jpg` layout). A Storage error is `console.warn`-ed and does NOT fail the action — the row is already gone, the orphan file is harmless (nothing references it), and inverting the order to delete the file first would risk a Storage success + RPC failure that strands the row with a dead `photo_url`. The intent is captured in a comment block above the function pointing at `removePersonPhoto`'s identical order-matters discipline. (2) `src/app/dashboard/actions.ts` `deleteTree` (lines 100–135) gains a Storage purge of the **whole `trees/<treeId>/people/` prefix BEFORE the row delete** — order matters here in the opposite direction: once `trees` is deleted the FK cascade kills `tree_members` for that tree, which flips `is_tree_editor(treeId)` to `false` and locks the caller out of the Storage INSERT/UPDATE/DELETE RLS policies sub-task 1 installed, leaving any surviving objects permanently unreachable. Implementation: `storage.from('photos').list('trees/<treeId>/people', { limit: 1000 })` → filter to folder entries via the documented Supabase Storage idiom **`entry.id === null`** (file entries carry a non-null `id`, prefix/folder entries carry `null` — this is how Storage's S3-style listing surfaces "directories") → map each folder name to its `trees/<treeId>/people/<name>/avatar.jpg` path → single batched `remove(paths)` call. Both list errors and remove errors `console.warn` and continue; neither failure mode aborts the tree delete (orphans are GC-able later, the user still gets their tree gone). v0.1 caps trees at ~200 people so the 1000-path single-call cap on `remove` is comfortably above the worst case. Verification: `pnpm typecheck` clean; `pnpm lint` 1 warning, pre-existing (`PersonForm.tsx` `react-hooks/incompatible-library` — unchanged by this sub-task); `pnpm test --run` 10 files / 86 tests pass (unchanged from sub-task 3; the new tests covering delete-cleanup behaviour land in sub-task 5). Branch: `feat/phase-5/sub-task-4-delete-cleanups`. *(commit SHA — backfill in a later commit per existing pattern)*
- [x] **Sub-task 5** — Tests + Phase 5 close-out. **21 new Vitest tests across 3 files**, suite total 107 passing locally (was 86 at sub-task 3). (1) `src/__tests__/rls/storage_photos.test.ts` — **12 tests** covering owner / editor / non-member / anon × INSERT / SELECT / UPDATE / DELETE on the `photos` bucket, plus the cross-tree negative (editor of T_A uploading into T_B is rejected). Uses a JPEG-shaped Blob (JFIF header + EOI marker) so the bucket's `allowed_mime_types = ['image/jpeg']` enforcement is satisfied at the storage-api layer — anything more handwavy (plain `Blob([0])`) gets rejected at MIME, not at RLS, and would hide policy regressions. The `afterAll` hook removes storage objects BEFORE the `auth.users` delete so the cascade doesn't strand the suite's owner-client; this ordering matters because once the user's gone the RLS gate flips and the test fixture can't reach its own objects to clean up. (2) `src/__tests__/actions/uploadPersonPhoto.test.ts` — **6 tests**: happy path returns a valid Supabase public URL AND writes `people.photo_url`; cross-tree owner-of-A uploading into B is rejected; empty Blob / oversized (524289 bytes — one byte over the 524288 lid, defense in depth matching the bucket's `file_size_limit`) / missing-`file`-key all rejected with the right error string; upsert replaces rather than duplicates. Mock pattern mirrors `createPerson-linkSpec.test.ts` — `vi.mock('next/cache', () => ({ revalidatePath: noop, refresh: noop }))` + `vi.mock('@/lib/supabase/server')` with a `clientHolder` that's swapped per-test so each case gets a freshly-scoped Supabase client without re-importing the action module. (3) `src/__tests__/actions/removePersonPhoto.test.ts` — **3 tests**: happy path nulls `photo_url` and removes the file; idempotent on already-null + no-file (action still returns `ok: true`); cross-tree owner-of-A trying to remove from T_B silently no-ops — the action returns `ok: true` because `people.update` RLS's USING-clause filters non-editor rows out of the result set, and the action's design treats Storage errors as logged-warnings-not-failures (per sub-task 2's order-matters discipline); the assertion is that T_B's `photo_url` is unchanged AND the file is still present. **Smoke flow `phase-5-photo-upload`** appended to [`../qa/smoke-flows.md`](../qa/smoke-flows.md) after the Phase 4 section — 10 numbered steps covering all 6 ship-gate bullets end-to-end (create tree → add person with initials → edit and pick a ≥5 MB fixture JPEG → optimistic blob preview within ~300 ms → Supabase URL within ~2 s via `refresh()` → resize keeps payload under 200 KB → submit → tree-canvas AND detail-sheet both show photo → create-flow upload via FAB → Remove photo → file gone from Storage → delete person → file gone → delete tree → entire prefix empty), plus an optional cross-tenant negative as step 11. Skip rules: `needs-local-supabase`, `needs-large-jpeg-fixture`, `needs-tools-grant-fix` (same Playwright-MCP block the Phase 3 + 4 close-outs called out). **`family-tree-prod` Supabase project created** — id `ycnsgkotrbjifsjkqmvn`, region `ap-south-1` (matches QA), URL `https://ycnsgkotrbjifsjkqmvn.supabase.co`. All 7 migrations replayed via `mcp__supabase__apply_migration` in timestamp order; `mcp__supabase__list_migrations` cross-check passes on (name, content, order); `mcp__supabase__list_tables` confirms `profiles`, `trees`, `tree_members`, `people` (all RLS-enabled, all 0 rows); `mcp__supabase__get_advisors` baseline matches QA modulo expected drift — 12 security WARNs (= QA baseline minus the `leaked_password_protection` toggle that requires a dashboard switch, not a migration) + 8 performance INFOs (2 unindexed FKs + 6 unused indexes — one more than QA's 5 because prod's index-usage stats are fresh-from-zero; expected, not drift). Storage `photos` bucket confirmed via `execute_sql` (public=true, file_size_limit=524288, allowed_mime_types=['image/jpeg']); all 4 RLS policies (`photos_insert_editor` / `photos_update_editor` / `photos_delete_editor` / `photos_select_editor`) installed. Vercel production env vars set out-of-band by the user (URL, anon key, service-role key all point at family-tree-prod). **`release/v0.1.0` cut from qa** — first named release per [ADR 0009 §1](../adrs/0009-versioning-and-releases.md). `pnpm version minor --no-git-tag-version` (`0.0.5` → `0.1.0`). Release [PR #33](https://github.com/SanchitB23/meetthefam/pull/33) into `main` merged with a **real merge commit** (`e00f21e`), NOT squash. Required a `Merge main into release/v0.1.0` resolver-commit (`4673f59`) first because main's v0.0.5 release bubble (#14 + hotfix #19 + #20) wasn't on qa — qa only had the squash-forward via #22; resolution: keep the release branch's `0.1.0`. GitHub Release [v0.1.0 prerelease](https://github.com/SanchitB23/meetthefam/releases/tag/v0.1.0) created with `--target main` so the tag points at `e00f21e` (no local tag exists, per the `--no-git-tag-version` discipline). Forward-[PR #34](https://github.com/SanchitB23/meetthefam/pull/34) squash-merged back into qa to return the version bump — qa's `package.json` is now `0.1.0`. Branch: `feat/phase-5/sub-task-5-tests-and-closeout`. *(commit SHA — backfill in a later commit per existing pattern)*

**Phase 5 close-out**:

- [x] All five sub-tasks ticked above. *(see commits above; sub-tasks 1–4 ticked in prior commits, sub-task 5 ticked in this commit)*
- [x] Per-sub-task docs ticks landed in `current-phase.md` + `phase-backlog.md` in the same commit as each feature commit (per the standing memory rule).
- [x] Vitest suite passing: **21 new Phase 5 tests** (12 RLS `storage_photos` + 6 `uploadPersonPhoto` + 3 `removePersonPhoto`) — total suite now **107 tests** (was 86 at sub-task 3; +21 in sub-task 5).
- [x] `e2e-smoke-tester` flow — **skipped**, same reason Phase 3 + 4 close-outs captured (`.claude/agents/e2e-smoke-tester.md` `tools:` grant fix still in the Tooling backlog); manual QA on the QA preview stood in for the `phase-5-photo-upload` flow.
- [x] Manual QA pass on the QA preview — user walked the pre-merge smoke checklist from release PR #33's body before marking it ready (covered sign-up / tree create / spouse + child / photo upload / Remove photo / delete person + delete tree with Storage state confirmed via Studio / mobile viewport).
- [x] Release version: **`v0.1.0`** — first named release; closes the v0.1 milestone (personal MVP). Per [ADR 0009 §1](../adrs/0009-versioning-and-releases.md) future Phase 6–8 closures each bump `pnpm version minor`; Phase 9 launch is `v1.0.0`.

> **Workflow note** — Phase 5 continued under the feature-branch workflow ([ADR 0010](../adrs/0010-feature-branch-workflow.md)). Each sub-task landed on its own Conventional-Commit-prefixed branch PR'd into `qa` with squash-merge; the release at the end of the phase rode a `release/v0.1.0` branch from `qa` into `main` (merge commit), then back into `qa` (squash) per [ADR 0009 §4](../adrs/0009-versioning-and-releases.md). Phase 5 closed the v0.1 milestone — first named release, `pnpm version minor` (0.0.5 → 0.1.0) — see [ADR 0009 §1](../adrs/0009-versioning-and-releases.md) table.

---

## Previous phase: 4 — Tree visualization (✅ closed)

Closed with **`v0.0.5`**. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.0.5).

**Ship gate (met)**:

- Open `/tree/[id]` for the Smith Family Demo seed → see all 13 people rendered as a real horizontal focus-person tree (no more grid). One-finger drag pans; pinch / scroll-wheel zooms.
- Tap any node → person detail sheet slides up on mobile / in on desktop with full bio + relations summary + "Edit" CTA.
- Long-press a node (500 ms, haptic where supported) OR tap a `…` affordance on the node → action menu opens with Re-center / Edit / Set spouse / Set parents / Add relative / Clear spouse / Delete.
- "Re-center here" updates both the visible focus AND `window.location.hash = '#p=<uuid>'`. Copy URL → open in another tab → tree opens already centered on that person (SSR via `?p=<id>` searchParams mirror).
- Floating "+" FAB is context-aware: with a focus set, says "Add a relative to <name>" and pre-seeds the linkSpec; with no focus, falls back to "Add a person".
- All Phase 3 link operations (set spouse, set parents with cycle rejection, add relative, clear spouse, delete with FK cleanup) still work end-to-end from the new action-menu entry point.
- RLS still holds — anonymous hit on `/tree/[id]` bounces to `/login` via `proxy.ts`.
- `pnpm typecheck && pnpm lint && pnpm test` clean (Phase 3 baseline + new Phase 4 unit tests for the data transform + press-actions hook).

**Sub-tasks (all closed)**:

- [x] **Sub-task 1** — Library install + data transform + smoke render. `pnpm add family-chart` (`family-chart@0.9.0`, pure JS — no `pnpm.onlyBuiltDependencies` change needed). New `_lib/types.ts` (moves `PersonRow` out of the deleted `_components/PersonCard.tsx` so non-card code can import the type without pulling card chrome). New `_lib/family-chart-data.ts` — pure `transformToFamilyChartShape(rows): FamilyChartDatum[]` matching the library's verified 0.9.0 `Datum` shape (`{ id, data: { gender: 'M'|'F', … }, rels: { parents, spouses, children } }`); derives children from `father_id`/`mother_id`, defensive spouse-array handling, gender mapping (`'m'|'other'|'unknown'` → `'M'`, `'f'` → `'F'`, truthful value preserved at `data.gender_raw`); Vitest unit coverage deferred to sub-task 6. New `_components/FamilyTree.tsx` (`'use client'`) wrapping `f3.createChart` with library-default `setCardHtml().setCardDisplay([['full_name'], ['birth_year']])` for the smoke, Strict-Mode-safe cleanup via `cont.innerHTML = ''`, full-height (100vh − 9rem) container with heirloom border + bg-card chrome — pan + zoom + library-default click-to-recenter all come for free. `page.tsx` swaps `<PersonList>` for `<FamilyTree people={people} />` and keeps the empty-state branch (family-chart can't render zero datums). Deletes `_components/PersonList.tsx` and `_components/PersonCard.tsx`; repoints `PersonRow` imports across `_lib/relations.ts`, `PersonForm.tsx`, `PersonPicker.tsx`, `PersonCardMenu.tsx`, `SetParentsDialog.tsx` to `_lib/types`. `PersonCardMenu.tsx` is currently unreferenced (its grid is gone); kept alive intentionally as the starting point for sub-task 4's `PersonActionMenu`. Folds in the [`../ux/tree-view.md`](../ux/tree-view.md) data-shape fix — the old `rels: { father, mother, … }` example is wrong for family-chart 0.9.0; replaced with the verified `rels: { parents, spouses, children }` shape + pointer to the live implementation file.
- [x] **Sub-task 2** — PersonNode go/no-go spike + custom HTML card. **Outcome: GO.** family-chart 0.9.0 exposes `setCardInnerHtmlCreator((d: TreeDatum) => string)` + `setCardDim({ w, h })` as first-class APIs on the `CardHtml` wrapper (verified at `node_modules/family-chart/dist/types/core/cards/card-html.d.ts:46`) — the planned `docs/adrs/0011-family-chart-default-node-fallback.md` is NOT created since no fallback was needed. New `src/app/tree/[id]/_lib/person-node-html.ts` — pure `personNodeHtml(d: TreeDatum): string` helper rendering a 158×110 wrapper with rounded heirloom border + `bg-card`, 48 px circular avatar at top (`<img>` when `data.photo_url`, otherwise tone-tinted serif initials via `var(--tone-${tone}-bg)` / `var(--tone-${tone}-ink)`), serif name line-clamped to 2 lines (`-webkit-line-clamp`), and a date line that conditionally renders `b. YYYY` / `b. YYYY – d. YYYY` / nothing based on `birth_year` × `death_year` × `deceased`. All user-input fields escaped through a small `escapeHtml()` helper since the API takes raw HTML. `src/components/ui/avatar.tsx` lightly edited to export the existing `computeInitials` so the React `<Avatar>` and the HTML template share one initials source (no behavioral change). `FamilyTree.tsx` swaps the smoke `chart.setCardHtml().setCardDisplay([['full_name'], ['birth_year']])` line for `chart.setCardHtml().setCardDim({ w: 158, h: 110 }).setCardInnerHtmlCreator(personNodeHtml)` and tightens chart spacing from the smoke's `250 / 150` to `220 / 130` per ADR 0008's PersonNode tuning notes. Deceased † badge, gender-shape avatar variation, floating "+" hover affordance, branch / leaf / sparkle decorations, and uppercase role label all explicitly deferred to Phase 8 polish (per ADR 0008) — same scope-fence the planning step called out. Vitest coverage of `personNodeHtml` (escape behaviour, deceased-line variants, photo-vs-initials branch) deferred to sub-task 6, matching the pattern sub-task 1 set for `transformToFamilyChartShape`. `pnpm typecheck` clean; `pnpm lint` 8 warnings — all pre-existing in `PersonForm.tsx`, none from new files. *(commit SHA — backfill in a later commit per existing pattern, see `eec4802` / `411b7f2` / `87919de`)*
- [x] **Sub-task 3** — Tap → person detail bottom sheet. `_lib/relations.ts` gains `buildRelations(person, peopleById)` — straight hoist of the helper from the deleted pre-Phase-4 `PersonCard.tsx` (recovered via `git show 2e3fc65^:src/app/tree/[id]/_components/PersonCard.tsx`), returning one-line summaries for spouse + parents; children listing intentionally NOT added (kept scope tight; polish item if user feedback wants it). Coexists with the `collectAncestors` / `collectDescendants` helpers Phase 3 sub-task 4 landed. New `_components/PersonDetailSheet.tsx` — `Sheet side="right"` on desktop, `side="bottom"` on mobile via `useIsDesktop`; header is a large `<Avatar size="lg">` + serif full_name with italic nickname (in quotes) rendered as the `SheetDescription`; body is an optional bio paragraph (`whitespace-pre-line`), a facts `<dl>` for dates / location / occupation (each row only rendered when the field is present), and the relations summary block; dates formatted as `b. YYYY` / `b. YYYY – d. YYYY` / `d. YYYY` to match the PersonNode card formatter; footer is a single "Edit" CTA. Edit flow captures the current person into a local `editPerson` state slot on click, closes the detail sheet, THEN opens `<PersonForm mode="edit">` on the captured row — using a separate state slot (not the detail-sheet's `person` prop) so closing the sheet doesn't unmount the form mid-transition, and avoiding the bad-UX of two stacked sheets on mobile by sequencing them. `FamilyTree.tsx` changes: new `treeId` prop (passed through to PersonDetailSheet → PersonForm); local `[detailPersonId, setDetailPersonId]` holds the tapped person id; `peopleById` Map is built client-side via `useMemo` on the `people` array (not passed from the Server Component, since `Map` doesn't survive JSON serialization across the RSC boundary); a `peopleByIdRef` ref tracks the latest Map so the once-registered `onCardClick` handler always sees fresh data without tearing the chart down on every revalidate; chart wiring becomes `chart.setCardHtml().setCardDim(...).setCardInnerHtmlCreator(...).setOnCardClick((e, d) => setDetailPersonId(d.data.id))` — `setOnCardClick` overrides family-chart's default click-to-recenter (sub-task 4 will move re-centering to the action-menu item so the user chooses what a tap means); the chart container + `<PersonDetailSheet>` are wrapped in a fragment so the sheet can portal correctly. `page.tsx` trivially edited to pass the new `treeId` prop to `<FamilyTree>` (the `peopleById` Map is built inside the client component, not on the server, per the JSON-serialization note above — deviates from the original planning bullet's wording in a deliberate, documented way). `pnpm typecheck` clean; `pnpm lint` 8 warnings — all pre-existing in `PersonForm.tsx`, none from new files. Vitest coverage of `buildRelations` + the detail-sheet field rendering deferred to sub-task 6, matching the pattern sub-tasks 1 + 2 set. *(commit SHA — backfill in a later commit per existing pattern, see `eec4802` / `411b7f2` / `87919de`)*
- [x] **Sub-task 4** — Long-press / "…" → action menu. New `_lib/usePressActions.ts` hook implementing the 500 ms tap-vs-long-press differentiation from [`../ux/mobile-gestures.md`](../ux/mobile-gestures.md), with `navigator.vibrate?.(10)` on long-press (no-op on Safari iOS per the spec, harmless on other engines). Implementation deviates from the planning bullet on one detail — uses **event delegation on the chart container** (single `pointerdown` / `pointerup` / `pointermove` / `pointercancel` listener that walks up to the nearest `[data-person-id]`) rather than per-node binding via family-chart's `onCardUpdate`. Functionally equivalent, simpler cleanup, and no leak risk if the library re-renders nodes underneath us. The hook ONLY fires `onLongPress` — taps stay with the library's `setOnCardClick` — and returns a `shouldSuppressNextClickRef` ref that the caller consumes inside its click handler so the pointerup that ends a long-press doesn't fire the detail sheet. 8 px drift threshold + `pointermove` cancellation so finger jitter doesn't kill the press; pointer captured at the capture phase so a future library-internal `stopPropagation()` won't break us. The hook ignores `pointerdown` events inside `[data-action-trigger]` so the three-dot button never accidentally starts a gesture. New `_components/PersonActionMenu.tsx` — a port of the deleted `PersonCardMenu`, with three deliberate changes: (1) **no own trigger** — open is fully controlled by the parent via an `anchor: { personId, x, y } | null` prop; the component renders a 1×1 invisible position-fixed `<button>` as the Base UI `DropdownMenuTrigger`, positioned at the supplied viewport coords — the popup anchors next to it. Same mechanism serves both entry points (gesture long-press at the node rect AND fallback three-dot click) without divergent code paths. (2) **"Re-center here" added** as the top item — calls back to the parent via `onRecenter(personId)`, which delegates to `chart.updateMainId(personId).updateTree()` in FamilyTree, closing the loop on sub-task 3's removal of the library's click-to-recenter (the user now explicitly chooses what a tap means: detail sheet via tap, re-center via the menu). (3) **Subordinate-dialog state outlives the menu's open state** — `spousePickerForId`, `parentsDialogForId`, `addRelativeForId`, `editForId`, `deleteForId` all live on the menu component, not inside the open dropdown; clicking "Set spouse" sets `spousePickerForId` and closes the menu, but the picker stays mounted because `PersonActionMenu` itself stays mounted across menu open/close cycles. Avoids the "menu unmounts → picker unmounts mid-interaction" race the original PersonCardMenu was structurally vulnerable to. `_lib/person-node-html.ts` updated — every card now carries `data-person-id="<uuid>"` on the wrapper (consumed by the press hook), and an `EllipsisVertical`-shaped `<button data-action-trigger>` overlay at the top-right with a 44×44 px hit area (Apple HIG min per [`../ux/mobile-gestures.md`](../ux/mobile-gestures.md)). The wrapper changed from `overflow: hidden` to `overflow: visible` so the button can extend slightly outside the 158×110 footprint without clipping; the inner name line keeps its own `overflow: hidden` so long names still ellipsize. Icon is inlined SVG (3 circles) rather than imported because the helper returns an HTML string — we can't mount React Lucide here. `FamilyTree.tsx` rewired: holds a `chartRef: Chart | null` so the re-center handler can call `chart.updateMainId(id).updateTree()` imperatively; new `[actionAnchor, setActionAnchor]` state plus the press hook; `setOnCardClick` now branches on `e.target.closest('[data-action-trigger]')` — match → open the action menu anchored at the button's `getBoundingClientRect().right / .bottom`; no-match → open the detail sheet (sub-task 3 behavior preserved); first line of the click handler consumes `shouldSuppressNextClickRef.current` to swallow the pointerup that ends a long-press; renders `<PersonActionMenu>` alongside the detail sheet and passes a `handleRecenter` that closes the menu and re-centers the chart. `_components/PersonCardMenu.tsx` deleted (its successor `PersonActionMenu.tsx` is wired in); stale comment references in `PersonPicker.tsx`, `SetParentsDialog.tsx`, `PersonForm.tsx` mentioning `PersonCardMenu` are historical and left untouched (no code imports them). `pnpm typecheck` clean; `pnpm lint` 8 warnings — all pre-existing in `PersonForm.tsx`, none from new/changed files. Vitest coverage of `usePressActions` (tap, hold-500ms, pointercancel via fake timers + JSDOM) deferred to sub-task 6, matching the pattern sub-tasks 1 / 2 / 3 set. *(commit SHA — backfill in a later commit per existing pattern, see `eec4802` / `411b7f2` / `87919de`)*
- [x] **Sub-task 5** — URL hash sync + context-aware FAB + memoization. `FamilyTree.tsx` rewired around hash-as-source-of-truth: new `initialFocusId?: string | null` prop fed from the page-level `?p=<uuid>` searchParams (SSR seed); `useSyncExternalStore` subscribes to `hashchange` and reads `#p=<uuid>` for the runtime source — this shape (vs the planning bullet's plain `useEffect` + `setState` pair) resolves the new React-19 `react-hooks/set-state-in-effect` lint rule that fired the first time we tried this; `currentFocusId = hashFocus ?? initialFocusId ?? null` so hash wins, SSR is fallback, null until "Re-center here" is explicit. Chart-init effect seeds `chart.updateMainId(seedFocus)` BEFORE `chart.updateTree({ initial: true })` so the first paint is already-centered (no flicker); a subsequent React → chart sync effect handles every later focus change, gated by an `initialMountRef` so it doesn't double-apply on first render. "Re-center here" writes the hash via `history.replaceState` (back-stack stays clean) and dispatches a synthetic `HashChangeEvent` so the `useSyncExternalStore` subscription propagates without a real URL change firing one; no-op case (hash already matches the picked id) calls `chart.updateMainId(id).updateTree()` directly. Component export wrapped in `React.memo(FamilyTreeImpl)` — defaults to reference-equality on `people`; manual memo, not React Compiler (matches the Phase 4 backlog defer-Compiler note). **`<ViewTransition>` defer-or-promote decision: DEFER to Phase 8 polish** — family-chart's built-in `setTransitionTime(800)` already animates re-centering well enough on mobile; layering React 19.2 view transitions on top doesn't justify the complexity right now; rationale baked into a top-of-file comment in `FamilyTree.tsx` and the Phase 8 backlog "If tree navigation animations weren't done in Phase 4, do them here" entry stays the carrier for the follow-on. New `_components/AddRelativeFab.tsx` — single component, two variants: `variant="fab"` (default) is the floating bottom-right button (48 px / `12×12`, exceeds 44×44 HIG min) and `variant="empty-state"` is an inline pill "+ Add the first person" for the empty-state branch; both render the same internal `<PersonForm mode="create">` and when `focusPerson != null` pre-seed a `linkSpec` with `defaultRelation: 'child'` so the linked-create relation radio (Phase 3 sub-task 5) appears already-selected as "Child of <name>"; aria-label/title swap between "Add a relative to <name>" and "Add a person" so screen-readers + tooltip surface the focus-aware behaviour. `page.tsx` adds `await props.searchParams` per ADR 0007's async-API contract, reads `p` to derive `initialFocusId`, passes it to `<FamilyTree>`, and (since the FAB is now owned inside `FamilyTree`) drops the sibling `<AddPersonControls>`; the empty-state branch renders `<AddRelativeFab variant="empty-state" focusPerson={null} treeId={tree.id} />` — same component as the populated case, different chrome. Deletes `_components/AddPersonControls.tsx` and `_components/AddPersonFab.tsx` (both replaced by `AddRelativeFab`). `pnpm typecheck` clean; `pnpm lint` 8 warnings — all pre-existing in `PersonForm.tsx`, none from new/changed files. Vitest coverage of the hash parser + memo behaviour deferred to sub-task 6, matching the pattern sub-tasks 1 / 2 / 3 / 4 set. *(commit SHA — backfill in a later commit per existing pattern, see `eec4802` / `411b7f2` / `87919de`)*
- [x] **Sub-task 6** — Tests + Phase 4 close-out. `pnpm add -D jsdom` (`jsdom@29.1.1`, pure JS — no `pnpm.onlyBuiltDependencies` change needed) to give the press-detector test a JSDOM environment. **Refactored `_lib/usePressActions.ts`** — split into a pure `attachPressDetector(container, options)` DOM function plus a thin React `usePressActions` hook that calls it inside `useEffect`. The split lets us unit-test the press-detection logic in JSDOM without pulling in `@testing-library/react` (kept dep surface small); hook public API is unchanged. New `src/__tests__/lib/family-chart-data.test.ts` — **11 Vitest tests** covering the spec items: children-derived-from-father/mother (3 tests), null parent filtering, defensive spouse handling (both balanced + one-sided), gender mapping (`f→F`, `m/other/unknown→M`), `gender_raw` truthful-value survival, 4-gen Smith-style chain round-trip, payload field carry-through for the PersonNode renderer, single-word + multi-word `full_name` splitting. Node environment (default vitest); no DOM, no Supabase. New `src/__tests__/lib/usePressActions.test.ts` — **9 Vitest tests** in `@vitest-environment jsdom` with `vi.useFakeTimers()`: 500 ms hold fires (timing-edge: 499 vs 500), early release no-fires, pointercancel aborts, drift-threshold abort (10 px > 8 px), drift-within-threshold survives (3 px jitter still long-presses), pointerdown outside `.mtf-node` no-ops, pointerdown inside `[data-action-trigger]` no-ops, cleanup removes listeners, override `longPressMs` test-seam works. **Smoke flow `phase-4-tree-visualization` appended** to [`../qa/smoke-flows.md`](../qa/smoke-flows.md) — 13-step happy path covering both gesture paths (long-press + three-dot), tap → detail sheet, "Re-center here" + URL hash sync round-trip (open hashed URL in a new tab → SSR-centered), context-aware FAB label-swap, pan/zoom, deletion → spouse-symmetry cleanup. Skip rules call out the still-unresolved `e2e-smoke-tester` tools-grant blocker (same workaround Phase 3 used — manual QA stands in until fixed). `pnpm test src/__tests__/lib/` — 20 / 20 new tests pass; combined Phase 3 (49) + Phase 4 (20) suite total is **69 tests**. Phase 3 suite requires `supabase start` and was not re-run in this session — caller exercises in CI / pre-release verification. `pnpm typecheck` clean; `pnpm lint` 8 warnings — all pre-existing in `PersonForm.tsx`, none from new files. Flips this file to "Phase 5 — Photo upload (planning)" with the v0.0.5 release-notes pointer. (No explicit "deferred Vitest coverage" carrier in [`phase-backlog.md`](phase-backlog.md) Phase 4 section — the per-sub-task deferral notes live on the sub-task descriptions above; the remaining unchecked Phase 4 backlog item is "Floating '+' hover affordance" which is explicitly deferred to Phase 8 polish per ADR 0008.) *(commit SHA — backfill in a later commit per existing pattern, see `eec4802` / `411b7f2` / `87919de`)*

**Phase 4 close-out**:

- [x] All six sub-tasks ticked above. *(see commits above; sub-tasks 1–5 ticked in prior commits, sub-task 6 ticked in this commit)*
- [x] Per-sub-task docs ticks landed in `current-phase.md` + `phase-backlog.md` in the same commit as each feature commit (per the standing memory rule).
- [x] Vitest suite passing: **20 new Phase 4 lib tests** (`family-chart-data` + `usePressActions`) — total suite now 49 (Phase 3) + 20 (Phase 4) = **69 tests**. Phase 3 RLS / RPC / action suite (49 tests) requires `supabase start`; not re-run in this session — caller will exercise in CI / pre-release verification.
- [x] `e2e-smoke-tester` flow — **skipped**, same reason Phase 3 close-out captured (`.claude/agents/e2e-smoke-tester.md:4` `tools:` grant missing Playwright MCP prefix; carried as the Tooling backlog item). Manual QA on the QA preview stands in.
- [ ] Manual QA pass on the QA preview — pending (controller will run after merge, before the `release/v0.0.5` cut).
- [x] Release version: **`v0.0.5`** standalone (Phase 4 alone — continues the pre-Phase-5 `0.0.x` patch-per-deploy convention from [CLAUDE.md](../../CLAUDE.md) "Releases" + [ADR 0009](../adrs/0009-versioning-and-releases.md)).

> **Workflow note** — Phase 4 was the first phase shipped under the feature-branch workflow ([ADR 0010](../adrs/0010-feature-branch-workflow.md)). Each sub-task and incidental fix landed on its own Conventional-Commit-prefixed branch (`feat/phase-4/sub-task-N-<slug>`, `fix/<slug>`, `chore/<slug>`, `docs/<slug>`, etc.), PR'd into `qa` with squash-merge. The release at the end of the phase rides a `release/v0.0.5` branch from `qa` into `main` (merge commit), then back into `qa` (squash) per [ADR 0009 §4](../adrs/0009-versioning-and-releases.md) — see CLAUDE.md → "Releases" for the full recipe.

---

## Previous phase: 3 — People CRUD + linking (✅ closed)

Closed with **`v0.0.4`**. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.0.4).

**Ship gate (met)**:

- Navigating `/dashboard` → tapping a tree card → lands on `/tree/[id]` showing the tree name + a person list (or empty state). Non-members get a 404 via RLS.
- Add a person via "+" FAB → row appears immediately (`revalidatePath`); tone auto-assigned by the DB trigger.
- Edit a person from the card menu → form prefills, save updates the row + the list; tone-override swatch works.
- Delete a person → confirmation modal → row removed, any inbound FKs (other rows pointing at it as spouse / father / mother) nulled atomically via RPC.
- Set spouse from the card menu → both `A.spouse_id` and `B.spouse_id` are set (bidirectional sync); clearing spouse nulls both sides.
- Set parents → ancestor-cycle detection rejects "make grandpa my son" with a clear error toast; the person-picker UI also guards obvious cases (self, descendants).
- Add child → opens the create form with parents prefilled (focus person + their spouse if any).
- Vitest RLS suite for `people` passes: non-members blocked on SELECT / INSERT / UPDATE / DELETE; editors can read + write; anon has no access. Spouse-symmetry + cycle-detection integration tests pass.

**Sub-tasks (all closed)**:

- [x] **Sub-task 1** — Page shell + `<Avatar>` + read-only person-list at `/tree/[id]`. Server Component using `PageProps<'/tree/[id]'>`; RLS-gated `trees` + `people` fetches; `notFound()` on missing tree; back-arrow + tree-name header; empty state with disabled "+ Add the first person" CTA (wired in sub-task 2). `<Avatar>` component per [`../ux/avatars-and-tones.md`](../ux/avatars-and-tones.md) (photo fallback → tone-tinted initials in Cormorant Garamond). `PersonList` grid + `PersonCard` (avatar + name + italic nickname + relations summary + birth/death years). Dashboard `<TreeCard>` switched from `#` to `/tree/${id}` using the stretched-link pattern. *(commit `c6552a6`)*
- [x] **Sub-task 2** — Add person (no linking). Installed shadcn `Sheet` (`src/components/ui/sheet.tsx`, hand-ported from the base-nova registry since `cn` + `Button` already existed locally). `PersonForm.tsx` (`react-hook-form`, Sheet on mobile / Dialog on desktop via a `useSyncExternalStore` media-query hook) — all 10 fields per [`../ux/add-edit-person.md`](../ux/add-edit-person.md) minus photo (deferred to Phase 5); conditional `death_year` via `watch('deceased')`. `AddPersonFab` floating "+" bottom-right (48×48, Lucide `Plus`); `AddPersonControls` owns shared `open` state for FAB + empty-state CTA. `createPerson(treeId, data)` Server Action trims strings → null, validates `full_name` (≤80), inserts with `tone: null` (DB trigger fills it), `revalidatePath('/tree/${treeId}')`; RLS gates via `people_insert_editor`. `react-hook-form` added to `package.json`. *(commit `1b01a1c`)*
- [x] **Sub-task 3** — Edit + delete person. `PersonForm` extended with `mode: 'create' | 'edit'` + optional `person` / `onSaved` props (edit-mode prefills via `valuesFromPerson()`, renders the 5-swatch tone-override radio with `role="radiogroup"` + `aria-checked` semantics, and renders a destructive Delete CTA that opens `<DeletePersonDialog>` inline). `PersonList` flipped to `'use client'` and owns shared `editId` state so a single edit-form instance services the whole grid — `PersonCard` stays presentational (option (c) from the plan, leaves room for `PersonCardMenu` in sub-task 4). `birth_date` is omitted from the submit patch when blank (the sub-task-1 `PersonRow` SELECT doesn't include it). `DeletePersonDialog` mirrors `DeleteTreeDialog` but uses `useTransition` to match the `{ ok, error }` shape of `deletePerson`. Migration `20260512111009_delete_person_rpc.sql` adds `delete_person_atomic(p_person_id)` — a `SECURITY INVOKER` single-statement `DELETE`; the schema's existing `ON DELETE SET NULL` on `father_id` / `mother_id` / `spouse_id` cleanly handles inbound-pointer nullification without firing the `people_touch_updated_at` trigger on partner rows. `updatePerson(personId, treeId, data)` Server Action writes a sparse patch (no-op early return when empty, `tone` validated at runtime against the 5-tone enum, `deceased` + `death_year` move together); `deletePerson(personId, treeId)` calls the RPC. *(commit `4fbb06d`)*
- [x] **Sub-task 4** — Linking after creation. Migration `20260512112332_people_link_rpcs.sql` adds three `SECURITY INVOKER` plpgsql functions: `set_spouse_atomic` (bidirectional symmetric bond, clears prior bond on either side, rejects self-spouse + cross-tree), `set_parents_atomic` (nullable args, ancestor-cycle check via recursive CTE walking `father_id` + `mother_id` from both new parents with `UNION` for termination on pre-existing cycles, rejects self-as-parent + cross-tree), `clear_spouse_atomic` (idempotent nulls both ends). All exceptions use `errcode = 'P0001'` with user-facing strings surfaced verbatim to the UI. Hand-ported shadcn `Command` primitive (`src/components/ui/command.tsx`) backed by new `cmdk@^1.1.1` dep (Base UI doesn't ship Command; cmdk is the shadcn-default across presets). Shared `useIsDesktop` hook extracted out of `PersonForm` to `src/components/ui/use-is-desktop.ts` for `PersonPicker` + `SetParentsDialog`. New components: `PersonPicker` (Sheet on mobile / Dialog on desktop, cmdk-powered search on `${full_name} ${nickname}` lowercased, exposes a `footer` slot for inline errors), `SetParentsDialog` (joint Father + Mother flow with dynamic exclusion sets so the same person can't fill both roles; state-reset via the render-phase "session key" pattern to satisfy `react-hooks/set-state-in-effect`), `PersonCardMenu` (per-card `...` dropdown — Set spouse / Set parents / Add child *(soon — sub-task 5)* / Clear spouse / Edit / Delete, computes exclusion sets via the new helpers). `_lib/relations.ts` adds `collectAncestors` + `collectDescendants` helpers with visited-set termination. `PersonList` refactored to the stretched-button pattern (relative wrapper, z-0 absolute `<button>` for tap-to-edit, z-10 card content with `pointer-events-none`, z-10 menu in top-right with `pointer-events-auto`) — avoids the button-inside-button invalid HTML the sub-task 3 pattern would have produced once the menu landed. `actions.ts` adds `setSpouse`, `setParents`, `clearSpouse` Server Actions calling the new RPCs by name. Manual matrix test against the seeded Smith Family Demo data passes (cycle / self-spouse / cross-tree / 4-gen cases); Vitest coverage of the rejection paths lands in sub-task 6. *(commit `9e51c12`)*
- [x] **Sub-task 5** — Linking at creation. `createPerson(treeId, data, linkSpec?)` extended with an optional `LinkSpecInput` discriminated union (`spouse | father | mother | child` + `focusPersonId`); after the row insert succeeds, a private `applyLinkSpec` helper composes the sub-task-4 RPCs by name — `set_spouse_atomic` for spouse, `set_parents_atomic` (preserving the focus's existing other parent slot via a pre-read) for father/mother, and `set_parents_atomic` again for child (reading focus `gender` + `spouse_id` to decide slots; `f` → mother slot, otherwise father slot — `other` / `unknown` deterministically default to father, documented as a single-rule choice; focus's spouse, if any, fills the opposite slot). If the linking RPC errors, the action returns `{ ok: false, error }` and the orphan row is intentionally left in place (silent DELETE judged worse UX — user can fix manually). `revalidatePath('/tree/${treeId}')` fires once at the end regardless of link success/failure. `PersonForm` extended with new exported `LinkRelation` + `LinkSpec` types and an optional `linkSpec` prop; when present, a "How is this person related?" radiogroup renders above the name field as a flat 4-option control (Spouse / Father / Mother / Child of `<focusPersonName>`) — flattened from the plan's 3-option-with-sub-question pattern so the user doesn't have to enter gender before relation. Controller-driven, `role="radiogroup"` + `role="radio"` + `aria-checked` semantics, heirloom-palette styling; form title + description swap to "Add a relative" in linked-create mode and the relation is passed through to `createPerson`'s `linkSpec` arg on submit. `PersonCardMenu` swaps the disabled "Add child *(soon)*" item for an enabled "Add relative…" item that owns its own `<PersonForm mode="create" linkSpec={{ focusPersonId, focusPersonName, defaultRelation: 'child' }} />` instance (state pattern mirrors the existing `spousePickerOpen` / `parentsDialogOpen` flags). No new SQL, no new components. `pnpm typecheck` + `pnpm lint` clean. *(commit `613c5dd`)*
- [x] **Sub-task 6** — Tests + close-out. **44 new tests across 6 files**, suite total 49 passing locally against `supabase start`. `src/__tests__/_helpers.ts` — shared fixtures (admin/anon Supabase clients, `signUpUserViaAdmin`, tree + person seeding helpers, direct `tree_members` inserts for editor-role fixtures). `src/__tests__/rls/people.test.ts` — **10 tests** covering owner / editor / non-member / anon × SELECT / INSERT / UPDATE / DELETE plus a "no auth header at all" case (mirrors the Phase 2 `trees.test.ts` pattern). `src/__tests__/rpc/delete_person_atomic.test.ts` — **6 tests** (spouse / father / mother / all-three inbound-FK cleanup; no-spouse no-op; non-existent uuid). `src/__tests__/rpc/set_spouse_atomic.test.ts` — **7 tests** (bidirectional symmetry / clear-A-prior / clear-B-prior / both-priors / re-affirm idempotency / self-spouse rejected / cross-tree rejected). `src/__tests__/rpc/set_parents_atomic.test.ts` — **10 tests** (both / father-only / mother-only / clear-both / self-father / self-mother / 2-gen cycle / 4-gen cycle / cross-tree / missing-target early-return). `src/__tests__/rpc/clear_spouse_atomic.test.ts` — **3 tests** (from-A / from-B symmetric idempotent / no-spouse no-op). `src/__tests__/actions/createPerson-linkSpec.test.ts` — **8 tests** (spouse-clears-prior / father-preserves-mother / mother-preserves-father / child-no-spouse / child-with-spouse / child-other-gender slot assignment / orphan-on-link-failure / cross-tree-spouse-rejected). `vitest.config.ts` gains a `resolve.alias` for `@/*` → `src/*` so the action test can import via the app's alias. Phase 3 `people-crud-and-link` flow (11 steps) appended to [`../qa/smoke-flows.md`](../qa/smoke-flows.md), mirroring the existing Phase 1/2 entry shape. *(commit `7f5f168`)*

**Phase 3 close-out**:

- [x] All six sub-tasks ticked above. *(see commits above)*
- [x] Per-sub-task docs ticks landed in `current-phase.md` + `phase-backlog.md` in the same commit as each feature commit (per the standing memory rule).
- [x] RLS + spouse-symmetry + cycle-detection Vitest tests passing locally (`pnpm test`) — 49 tests green. CI gating not enforced yet — see Tooling backlog "GitHub Action: pnpm test on PR" item.
- [x] Post-sub-task polish: card heights / menu padding / future-date validation in `24f0dea`; gender filter on parent pickers in `e47598d`; intermediate `h-full` wrapper fix in `4e9fef8`. QA Supabase migrations (`delete_person_rpc` + `people_link_rpcs`) applied directly to the QA project `ljjvwtpifmoshfknlbaj` via the Supabase MCP; local repo migrations match.
- [x] `e2e-smoke-tester` flow — **skipped** because the agent's `tools:` grant in `.claude/agents/e2e-smoke-tester.md:4` is missing the Playwright MCP prefix. First real dispatch (2026-05-12) returned BLOCKED. Captured as a Tooling backlog item (commit `852527f`); not a product defect. Manual QA on the qa preview stood in.
- [x] Manual QA pass on the qa preview (`meetthefam-git-qa-sanchit-bhatnagars-projects.vercel.app`) confirmed by user 2026-05-12 — all Phase 3 surfaces (people CRUD, set spouse, set parents with cycle rejection, set parents with new gender filter, tone override, even card heights) verified working post-fix.
- [x] Release version: **`v0.0.4`** standalone (Phase 3 alone — followed pre-Phase-5 `0.0.x` patch-per-deploy convention from [CLAUDE.md](../../CLAUDE.md) "Releases" + [ADR 0009](../adrs/0009-versioning-and-releases.md)).

---

## Previous phase: 2 — Tree CRUD + dashboard (✅ closed)

Closed with **`v0.0.3`**. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.0.3).

**Ship gate (met)**:

- Logged-in user on `/dashboard` sees every tree they own or are a member of, plus a role badge (owner / editor) and a "+ New tree" CTA. Empty state when none.
- "+ New tree" → modal (Sheet on mobile, Dialog on desktop) → create works end-to-end. New tree appears immediately on the list — no hard reload (per [ADR 0007](../adrs/0007-nextjs-16-and-async-idioms.md); currently via `revalidatePath('/dashboard')`, `updateTag` deferred — see Phase 2 backlog).
- "…" menu on an owner-card → Rename works. Updated name appears immediately.
- "…" menu → Delete with a destructive-styled confirmation works. Tree disappears from list immediately. FK `ON DELETE CASCADE` on `tree_members` (and on `people` once Phase 3 lands rows) means the rest cleans up automatically.
- "…" menu is **hidden on editor-cards** — non-owners can't see Rename / Delete.
- **RLS holds**: a non-owner attempting `UPDATE` / `DELETE` on a tree row is blocked at the database, even if the UI is bypassed. Cross-tenant isolation proven by Vitest (`src/__tests__/rls/trees.test.ts`, 4 tests passing).
- Mobile breakpoint = 1-col card grid; desktop = multi-col (`grid gap-4 sm:grid-cols-2 lg:grid-cols-3`). Reference: [`../ux/inspiration/kintree/`](../ux/inspiration/kintree/) → "Dashboard" screen.

**Sub-tasks (all closed)**:

- [x] **Sub-task 1** — Dashboard list page (read-only). Server Component reading `trees` joined to `tree_members` filtered to the current user's memberships; card per tree with name, description, role badge; empty state with disabled CTA. Sign-out moved into a top-nav slot under the new `/dashboard` layout. *(commit `9794295`)*
- [x] **Sub-task 2** — Create tree. "+ New tree" CTA → `Sheet` on mobile / `Dialog` on desktop. `createTree(formData)` Server Action calls `create_tree_with_owner` RPC (atomic insert into `trees` + owner row in `tree_members`). Dashboard list refreshes via `revalidatePath('/dashboard')`. *(commit `1a87c84`)*
- [x] **Sub-task 3** — Owner rename + delete. "…" menu on owner-cards only; `renameTree(treeId, newName)` + `deleteTree(treeId)` Server Actions, both gated by RLS `UPDATE` / `DELETE` policies. First Vitest RLS test landed at `src/__tests__/rls/trees.test.ts` — non-owner session blocked on `SELECT`, `UPDATE`, and `DELETE`. *(commit `46441ba`)*

**Phase 2 close-out**:

- [x] RLS Vitest test for `trees` / `tree_members` — user A cannot `SELECT` / `UPDATE` / `DELETE` user B's rows. *(commit `46441ba` — 4 tests in `src/__tests__/rls/trees.test.ts`)*
- [x] Per-sub-task docs ticks landed in `current-phase.md` + `phase-backlog.md` in the same commit as each feature commit.
- [x] Confirm `revalidatePath` cache-invalidation works on QA (not just local). Smoke: open `/dashboard` in two tabs, create a tree in tab 1, refresh tab 2 — verified by user 2026-05-12 against `meetthefam-git-qa-sanchit-bhatnagars-projects.vercel.app`.
- [x] Confirm mobile layout (1-col card grid) on QA via Chrome devtools mobile emulator — verified by user 2026-05-12.

See [ADR 0007](../adrs/0007-nextjs-16-and-async-idioms.md) for the async-idioms baseline (currently using `revalidatePath`; `updateTag` deferred until `"use cache"` adoption post-v0.1).

---

## Previous phase: 1 — Auth (✅ closed)

Closed with **`v0.0.2`**. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.0.2).

**Ship gate (met)**:

- Magic-link sign-in (from Phase 0) still works end-to-end on local + QA.
- Google OAuth one-click sign-in works end-to-end on local + QA.
- Hitting `/dashboard` while unauthenticated triggers an **edge-side** 307 from `proxy.ts` (not an in-page redirect).
- Hitting `/share/<random-token>` does NOT trigger the auth boundary (matcher skips it).
- "Sign out" button on `/dashboard` clears the session and returns to `/login`.

**Sub-tasks (all closed)**:

- [x] **Sub-task 1** — Google OAuth secondary sign-in. "Continue with Google" button + `signInWithGoogle` server action; `[auth.external.google]` block in `supabase/config.toml` with explicit `redirect_uri` (the empty default broke local with "missing redirect URI"); reuses the existing PKCE `/auth/callback` route. Verified local + QA. *(commit `fad1bb7`)*
- [x] **Sub-task 2** — `proxy.ts` auth boundary at `src/proxy.ts` (Node runtime, refreshes session via `@supabase/ssr`, 307s unauthenticated traffic to `/login`, matcher skips `/share/[token]`). Retired the in-page redirect from the dashboard. *(commit `3f1cee8`)*
- [x] **Sub-task 3** — Sign-out. `signOut()` server action + `<SignOutButton />` client component with `useFormStatus` pending state. *(commit `9c8e4e9`)*

**Phase 1 close-out** (all done):

- [x] Per-sub-task docs ticks landed in `current-phase.md` + `phase-backlog.md` in the same commit as each feature commit.
- [x] QA verification of all three sub-tasks via Chrome (`Sanchit Personal` profile) — Google OAuth → `/dashboard`, sign-out → `/login`, magic-link confirmation card.
- [x] Backlog reconciliation + "Standing rules" section added in `phase-backlog.md`. *(commit `68af64c`)*

See [ADR 0004](../adrs/0004-magic-link-only-no-passwords.md) for the passwordless-only auth stance and [ADR 0007](../adrs/0007-nextjs-16-and-async-idioms.md) for the `proxy.ts` over `middleware.ts` rationale.

---

## Previous phase: 0 — Foundation (✅ closed)

Closed with **`v0.0.0`** — the project's conventions baseline tag. See [release notes](https://github.com/SanchitB23/meetthefam/releases/tag/v0.0.0).

**Ship gate (met)**:

- `pnpm install && pnpm dev` boots Next.js at `http://localhost:3000`.
- `supabase start` brings up the local Postgres + Auth + Storage stack.
- Migration applied locally has all four tables (`profiles`, `trees`, `tree_members`, `people`) + RLS policies.
- QA Supabase project exists on the free tier; Vercel preview off `qa` reads from it.
- Logged-in placeholder page (`/dashboard`) shows a real authenticated user from the DB.
- All 5 Tier 1 MCPs (`supabase`, `context7`, `github-meetthefam`, `vercel`, `nextjs-devtools`) connect from a fresh `claude` session.

**Sub-tasks (all closed)**:

- [x] **Sub-task 1** — Next.js 16 scaffold (App Router + TS + Tailwind v4 + ESLint + `src/`; Node pinned to 24.15.0). *(commit `34d1aa4`)*
- [x] **Sub-task 2** — shadcn/ui init (`base-nova` preset on Base UI); heirloom-journal OKLCH palette + Cormorant Garamond / Manrope fonts. *(commit `0dd1ae6`)*
- [x] **Sub-task 3** — Local Supabase stack via `supabase init` + `supabase start`; CLI 2.98.2 installed as dev-dep with `pnpm.onlyBuiltDependencies` whitelist. *(landed in `846cdfb`)*
- [x] **Sub-task 4** — First migration via the **`supabase-engineer`** subagent: `profiles`, `trees`, `tree_members`, `people` + RLS policies + `tone` trigger + profile-on-signup trigger + Smith Family Demo seed (13 people). RLS smoke-checked. *(commit `846cdfb`)*
- [x] **Sub-task 5** — QA Supabase project `meetthefam-qa` (`ljjvwtpifmoshfknlbaj`, `ap-south-1`); single Vercel project with branch-targeted env vars. QA preview READY at `meetthefam-git-qa-sanchit-bhatnagars-projects.vercel.app`, production at `meetthefam.vercel.app` + `mtf.sanchitb23.in`. *(commit `ce72df9`)*
- [x] **Sub-task 6** — Magic-link login → `/dashboard` showing user's email; PKCE flow via `/login` → `/auth/callback` → cookie set → server-side `getUser()`. UX polish: `useFormStatus` spinner + email in confirmation card. *(commits `7e5346b`, `d48d395`)*

**Phase 0 close-out** (all done):

- [x] Drop `--turbopack` flag from scripts (never added — `34d1aa4`).
- [x] Add `engines.node` ≥ 24.15.0 to `package.json` (`34d1aa4`).
- [x] Add Next.js Devtools MCP to `.mcp.json`; bump Tier 1 MCP table from 4 → 5 (`f107e7b`).
- [x] Update `docs/setup/mcp-servers.md` with the Devtools MCP install command (`f107e7b`).
- [x] Verify `claude mcp list` shows all 5 Tier 1 MCPs connected from a fresh session (user-confirmed).
- [x] Wire `@vercel/analytics` + `@vercel/speed-insights` in `src/app/layout.tsx` (`f107e7b`).
- [x] Palette refinement to match Kintree per [ADR 0008](../adrs/0008-design-system.md) — `globals.css` migrated to two-tone + 5 TONES CSS vars.
- [x] Conventions baseline: `qa → main` workflow + phase-anchored SemVer + release process pinned. *(commit `dd1c3e1`, tagged `v0.0.0`)*

See [ADR 0007](../adrs/0007-nextjs-16-and-async-idioms.md) for the Next.js 16 idioms baseline and [ADR 0009](../adrs/0009-versioning-and-releases.md) for the release process Phase 0 established.

---

## Previous phase: −1 — Project AI infrastructure (✅ closed)

Set up Claude Code to be productive in this repo before any feature work — no app code, just docs, MCPs, agents, direnv. Ship gate verified in a fresh session.

- [x] Confirmation gates 1–3: work dir, GitHub repo, SSH alias.
- [x] Memories: "always ask before commit" + "strict work/personal GitHub separation."
- [x] **Mini-task 1** — repo skeleton (`0ffeebc`).
- [x] **Mini-task 2** — knowledge base: `CLAUDE.md` + `docs/` tree (`8dd64eb`).
- [x] **Mini-task 3 + 4** — project subagents + initial `.mcp.json` (Tier 1) + `docs/setup/mcp-servers.md` (`0765c5f`).
- [x] **Mini-task 5** — GitHub setup (repo created, SSH remote, push, direnv, `.mcp.json` cleanup).
- [x] Phase −1 ship-gate verification — fresh `claude` session: CLAUDE.md auto-loads, `claude mcp list` shows `supabase`, `context7`, `github` connected, agents visible, schema doc readable. `/doctor` env-var warnings confirmed cosmetic.
- ~~PAT rotation~~ — not needed; existing fine-grained PAT is already scoped only to `SanchitB23/meetthefam`.
