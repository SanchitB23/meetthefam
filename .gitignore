# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Testing
coverage/
playwright-report/
test-results/
# @playwright/mcp runtime output (console logs, page snapshots from e2e-smoke-tester dispatches)
.playwright-mcp/

# Next.js
.next/
out/

# Production builds
build/
dist/

# Environment files
.env
.env*.local
!.env.local.example

# Vercel
.vercel/

# Supabase
supabase/.branches/
supabase/.temp/
# Maintainer's personal-tree seed (per docs/adrs/0008-design-system.md → "Seed data").
# Smith Family Demo lives in the committed supabase/seed.sql; this is the local-only override.
supabase/seed.local.sql

# Superpowers brainstorm artifacts
.superpowers/

# IDE
.vscode/
.idea/
.DS_Store

# Claude Code per-machine local settings (project agents + .mcp.json ARE committed)
.claude/settings.local.json
# Claude Code runtime artifacts (ScheduleWakeup lockfile etc.)
.claude/*.lock
# Claude Code worktree-subagent isolation checkouts (per-machine, never committed)
.claude/worktrees/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local TLS / SSL files
*.pem
*.key

# Misc
*.tsbuildinfo
next-env.d.ts
